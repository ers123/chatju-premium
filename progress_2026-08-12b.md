# SoMyung Progress — 2026-08-12 (2차)

작성 기준: 2026-08-12 KST
브랜치: `codex/somyung-launch-ready` (= PR #10)
직전 문서: `progress_2026-08-12.md`

---

## 1. 한 줄 요약

**직전 문서 §6.1이 "사람이 눈으로 봐야 한다"고 남겨 둔 QA를 실제로 수행했고, 거기서
나온 결함 7건을 고쳐 배포했다.** 그 과정에서 예정에 없던 두 가지를 발견했다 —
**태국어와 중국어 PDF가 글자를 빈 상자로 인쇄하고 있었다.** 둘 다 유료 로케일이고,
둘 다 ja·es만 봐서는 영원히 안 나왔을 결함이다.

---

## 2. 이번 세션에서 확정된 사실

### 2.1 프리뷰와 유료 리포트가 서로 다른 아이를 말하고 있었다

무료 프리뷰는 **오행 개수 최다**로 "핵심 기질"을 내걸고, 유료 리포트의 부모 대사는
**일간(日干)** 을 기준으로 고른다. 두 기준을 864개 원국에 대해 대조했다.

| 항목 | 값 |
|---|---|
| 두 기준이 어긋나는 비율 | **59.7%** (516/864) |
| 최다 오행이 동점이라 키 순서로 결정되던 비율 | **30.7%** (265/864) |

QA 사례가 정확히 그랬다. 프리뷰는 「火の気質 — 情熱的・リーダーシップ」라고 약속했는데,
유료 리포트는 일간 壬水 기준이라 「大人しいから悩みがない」「考えてないで、まず動きなさい
(피할 말)」 같은 **내향형 아이 대사**가 나왔다. 돈을 낸 부모가 다른 아이 얘기를 받는다.

**결정: 일간으로 통일하고 프리뷰를 고쳤다.** 일간이 이 체계에서 '나 자신'이고, 유료
리포트가 처음부터 쓰던 기준이다. 개수 기반은 "이 원국에 무엇이 많은가/부족한가"를
묻는 자리(보완표·조절표)에만 남겼다.

> 여기서 계측기를 한 번 틀렸다. 첫 측정은 오행 이름을 한쪽은 한글(`목/화`), 한쪽은
> 영어(`wood/fire`)로 비교해 **전부 불일치로 셌다.** 직전 문서 §8.1의 교훈이 그대로
> 반복됐다 — 측정값을 믿기 전에 계측기부터 의심할 것.

### 2.2 태국어 PDF는 숫자와 라틴 문자를 전부 잃고 있었다

`NotoSansThaiLooped-Regular.ttf`의 글리프 테이블을 직접 조회했다. 추정이 아니다.

```
numGlyphs: 200
"A" MISSING  "z" MISSING  "0" MISSING  "9" MISSING  "木" MISSING  "·" MISSING
"ก" HAS      "%" HAS      "/" HAS
```

이 폰트에는 **라틴 문자도, 숫자도, 한자도 없다.** 결과:

| 대상 | 실제 출력 |
|---|---|
| 푸터 | `☯ SoMyung \| somyung.cc \| 3 / 17` → `▯ ▯▯▯▯▯▯▯ \| ▯▯▯▯▯▯▯.▯▯ \| ▯ / ▯▯` |
| 오행 분포 | `ไม้ (木) 1 · 13%` → `ไม้ ( ) %` |
| 기둥 아래 오행 | `火+金` → `+` |
| 본문 숫자 | `10–20 นาที` → `▯▯–▯▯ นาที` |

### 2.3 중국어 PDF는 간체자를 잃고 있었다

중국어는 일본어 폰트(`NotoSansJP`)를 쓰고 있었는데, 이 폰트에 간체 전용 자형이 없다.
**PDF의 정적 라벨만 세어도 글리프 없는 문자가 32종**이었다.

```
时 报 读 说 话 计 结 观 译 质 图 阅 这 值 输 围 为 设 发 续 语 预 给 误 对 实 验 边 动 际 绪 级
```

`时柱`는 차트 페이지 열 제목이고 `报告`는 문서 제목에 있다. AI 본문은 세지도 않은 수다.

### 2.4 실제로는 세 언어만 팔리고 있다

프로덕션 DB 집계다. 이 숫자가 이번 세션의 우선순위 판단 근거였다.

| 항목 | 값 |
|---|---|
| 실제 리포트(테스트 제외) | **24건 — ko 12 / ja 6 / en 6** |
| 완료된 결제 | **5건, 전부 USD** |
| zh·th·fr·es·vi·id·pt 실제 리포트 | **0건** |

지난 이틀간의 다국어 작업과 오늘의 폰트 수정은 전부 **아직 오지 않은 고객**을 위한
것이다. 결함 자체는 심각했지만 현재 피해자는 0명이다.

---

## 3. 한 일

### 3.1 ja·es 사람 눈 QA (직전 문서 §6.1)

샌드박스(`backend/.env.sandbox-test` :3001 + `frontend/scripts/sandbox-serve.sh` :8080)로
일본어는 입력 마법사 5단계를 전부 클릭해서, 스페인어는 같은 UI 코드 경로이므로
`calculate-promo`로 리포트 본문·PDF를 검증했다.

**정상 확인**: ja·es 모두 `presentationStatus: ready`(강등 없음), 한국어 0자, PDF 16·17쪽
정상, 악센트·간지 정확. 직전 세션에서 고친 PDF 결함(#6 한국어 액자, #7 한글 오행명)은
유지되고 있다.

**발견한 결함 7건**

| # | 결함 | 처리 |
|---|---|---|
| A | 프로모 오류가 9개 비한국어에 **한국어로 노출** (「유효하지 않은 프로모 코드입니다.」) | 수정 |
| B | 프리뷰와 리포트의 기질 기준 불일치 (§2.1) | 수정 |
| C | 최다 오행 동점 30.7%가 키 순서로 결정 | B 수정으로 표제에서는 해소 |
| D | 표지 날짜가 UTC — KST/JST 00~09시 생성 시 **전날** | 수정 |
| E | PDF에 `CALCULATED PROFILE` 영어 라벨 | 수정 |
| F | 필수 미입력 시 버튼만 비활성, 이유 표시 없음(부모 출생시간) | 미수정 |
| G | 날짜를 `2017.06.14`로 표기 — 한국식 점 표기를 전 언어 공용 | 미수정 |

A는 서버가 언어와 무관하게 한국어를 반환하는데 **프론트가 그 문자열을 현지화 문자열보다
우선**했다(`results/page.tsx`). 순서만 뒤집어 해결했다. 서버의 한국어 문자열 10개는
그대로 남아 있다(ko UI와 로그용).

### 3.2 기질 기준 통일 (`ff7ec01`)

- 프론트: `getCoreElement()` 도입 — `fourPillars.일주.천간오행`을 쓴다. 일주가 없을 때만
  개수 기반으로 떨어진다. 공유 카드·형제 비교 링크도 같은 값을 쓴다.
- 백엔드 프리뷰 프롬프트: `childCore`/`parentCore`(일간) 도입. "주 오행" 라벨은
  "원국에 가장 많은 오행"으로 정직하게 바꿨다.
- 백엔드 유료 프롬프트: `주 기질`을 `dayMasterElement` 기준으로. `부 기질`은 일간과
  겹치면 출력하지 않는다.
- **정형 문구를 10개 언어 전부 고쳤다.** 「水の気質が最も強い」는 水 1개·火 3개인 원국에서
  거짓이다. "타고났다" 계열로 바꿨다(en `was born with a`, ko `기질을 타고났습니다` …).

### 3.3 표지 날짜 (`ad9b03c`)

`toISOString()`은 항상 UTC다. `utils/report-date.js`를 새로 만들어 독자 타임존(프론트가
입력 단계에서 보내는 IANA 값)으로 포맷한다. 없으면 서비스가 다른 곳에서 이미 쓰는
기본값 `Asia/Seoul`로 떨어진다 — **UTC로는 떨어지지 않는다. 아무에게도 맞지 않는 값이라서.**

`ready` 리포트는 PDF도 `presentation.cover.date`를 쓰므로 한 곳을 고치면 웹·PDF가 함께
고쳐진다. fallback 리포트만 PDF의 자체 포맷을 탄다.

### 3.4 한자 각주 (`8eb3fea`)

프랑스어 부모에게 `丁酉 丙午 壬申 甲辰`은 해독 불가능한 기호다. 표 아래에 한 줄을 넣었다 —
각 기둥이 천간+지지이고, 작은 줄이 그 짝이 지닌 오행이며, 오행 이름은 아래 분포표에 있다는
안내. **ko·ja·zh는 그 글자를 읽으므로 각주가 붙지 않는다**(라벨 미정의 시 렌더 생략).

### 3.5 태국어 폰트 (`e5068ae`)

pdfkit에는 폰트 폴백이 없다. 태국어 문서에서만 `doc.text`를 감싸 **문자종별로 폰트를
나눈다** — 태국 문자(U+0E00–U+0E7F)는 Thai 폰트, 나머지는 `NotoSansJP`(라틴·숫자·한자·☯
전부 보유). 두 폰트가 정확히 상보적이라 글리프 없는 문자가 남지 않는다. 혼합 문자열은
pdfkit의 `continued`를 써서 줄바꿈·wrapping을 보존한다. 공백은 중립으로 처리해 태국어
문장이 띄어쓰기마다 쪼개지지 않게 했다.

**비태국어 문서는 래퍼에 진입조차 하지 않는다 — 프랑스어 PDF가 수정 전후 바이트 동일.**

### 3.6 죽은 폰트 55MB 제거 (`1d54c2c`)

`assets/fonts`가 77MB인데 코드가 참조하는 것은 22MB뿐이었다. `NotoSansCJKkr-Regular/Bold`
(32MB), `NanumGothic/Bold`, `NotoSansKR.ttf`, `NotoSansJP.ttf` — 여섯 개 모두 `FONT_MAP`이
만드는 경로에 없다. 배포 zip이 **47.3MB → 34.4MB**.

**이걸 먼저 안 뺐으면 중국어 폰트를 넣는 순간 50MB 한도를 넘어 배포가 실패한다.**

### 3.7 중국어 폰트 (`50be1b5`)

`NotoSansSC-Regular/Bold.otf`(SIL OFL, `notofonts/noto-cjk`의 `Sans/SubsetOTF/SC`,
8.33MB + 8.54MB). 태국어와 달리 **분할이 필요 없다** — 간체·라틴·숫자·☯·기둥 한자를
한 폰트가 전부 커버한다.

**여기서 함정 두 개를 밟을 뻔했다.**

1. `serverless.yml`에 `!assets/fonts/*.otf` 제외 규칙이 있었다. 예전 32MB CJK OTF를 빼려고
   넣은 것인데, 그대로 뒀으면 **로컬은 완벽하고 라이브만 빈 상자**인 배포가 나간다.
   폰트가 없으면 코드가 조용히 `ja`로 폴백하기 때문에 에러도 안 난다.
2. 그 규칙을 지우니 zip이 48.5MB — 한도까지 1.5MB. `node_modules`의 소스맵·`.d.ts`·
   README(합계 18.5MB, 런타임에 로드되지 않는다)를 제외해 **43.7MB**로 내렸다.

폰트는 `git add -f`로 강제 추적시켰다. `assets/fonts`가 gitignore라서, 새로 클론하면
폰트 없이 배포되어 같은 방식으로 조용히 깨진다.

### 3.8 운영 정리

프로덕션 `readings` 34건 중 **10건(29%)이 테스트 행**이었다(오늘 QA 4건 + 3/20~4/1 잔여
6건). 직전 문서의 "3건"은 과소 집계였다. 삭제 전 전체 컬럼을 JSON으로 백업했다.

```
readings    34 → 24
promo_usage 18 → 10   (readings 10건과 짝이 맞는 8건)
```

백업과 스크립트: `output/db-backups/` (gitignore).

---

## 4. 배포와 검증

백엔드 3회 배포(수정 → 폰트 제거 → 중국어 폰트), 매번 `verify:live` **14/14 PASS**.
프론트엔드 1회 배포(Cloudflare Pages, 812 파일).

**라이브 실측**(somyung.cc):

- 일본어 프리뷰가 `💧 核心気質 水の気質`, 「水の気質を持って生まれています」로 출력.
  AI 본문도 「（壬申＝日干中心）」라고 근거를 밝힌다.
- 잘못된 프로모 코드 → 「無効なプロモコードです。」, 한국어 0자.
- **프로덕션에서 중국어 리포트를 실제 생성해 PDF를 받아 이미지로 확인**했다.
  `推算档案` `时柱` `五行分布` `如何阅读这些计算结果` `解读范围` 전부 정상.
  로컬 검증만으로는 §3.7의 함정 1을 잡을 수 없으므로 이 단계가 필수였다.

### 4.1 부수 관측 — 정상 경로가 503을 한 번 거친다

프로덕션 리포트 생성이 30초를 넘겨 **API Gateway가 503**을 반환했다(Lambda 타임아웃 60s >
게이트웨이 한도 30s, 배포 로그에도 경고가 뜬다). 프론트의 claim key 폴링이 이를 받아내도록
설계돼 있고 실제로 `status: complete`로 회수됐다 — **사용자 피해는 없지만 정상 경로가
매번 503을 한 번 지나간다.** 모니터링에서 이 503을 장애로 오인하지 말 것.

---

## 5. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포 (`50be1b5`), `verify:live` 14/14, 패키지 46MB |
| 프론트엔드 | 최신 배포 (Cloudflare Pages) |
| 백엔드 테스트 | **175건** (11 suites) |
| 미커밋 | 0 |
| 폰트 로케일 매핑 | ko/en/vi/id/es/pt/fr → NotoSansKR · ja → NotoSansJP · **zh → NotoSansSC** · th → Thai+JP 분할 |
| 정리 대기 | 테스트 행 3건 (아래 §6.3) |

---

## 6. 남은 일

### 6.1 사람이 해야 하는 것

**테스트 행 3건 삭제.** 하네스의 자동 승인 분류기가 프로덕션 DB 삭제를 막아 실행하지 못했다.

```bash
cd backend && APPLY=1 BACKUP_PATH=../output/db-backups/test-rows-deleted-2026-08-12b.json \
  node ../output/db-backups/cleanup-test-rows.js
```

대상은 `qa-fr-legend@` / `qa-th-legend@` / `qa-zh-live@`. `APPLY=1`을 빼면 드라이런이다.

### 6.2 기술 과제 — 지금은 권하지 않는 것

직전 문서 §6.2의 두 항목은 **§2.4의 사용자 분포를 보고 나면 우선순위가 낮다.**

- **루브릭 런타임 게이트**: 리포트마다 영구적으로 비용·지연이 붙는 유일한 항목인데,
  막아야 할 불량이 관측된 적이 없다(fallback 6%, ja는 0/6). 게다가 현재 임계값은
  프랑스어가 떨어지도록 설계돼 있어 켜면 fr이 재생성 루프를 돈다.
- **vi 재판정**: ON의 천장이 56~63%이고 vi OFF 중앙값이 이미 42.5다. 잘해야 한 언어가
  +15pt, 대상 사용자 0명, 측정 비용은 생성×채점 반복.

### 6.3 기록만 해 두는 것

- **`fortuneCycles`에 한국어 문장 121개 필드**가 비한국어 클라이언트로 전송되고 DB에
  저장된다. 프론트·PDF 어디서도 렌더링하지 않아 **지금은 사용자에게 안 보인다.**
  이 필드를 쓰는 화면이 하나라도 생기면 즉시 유출이다.
  (직전 문서 §2.3의 "유출 출처 두 곳"은 *렌더링된 출력* 기준으로는 여전히 맞다.)
- 결함 F(비활성 버튼 사유 미표시), G(전 언어 공용 점 표기 날짜)
- 부모 나이 하한 20세가 의도된 선택인지 확인 (2026-08-11 §5부터 이월)
- `frontend/.env.cloudflare`의 만료된 토큰 삭제 (2026-08-11 §7.2부터 이월)
- staging Supabase 삭제 상태 (이월)

---

## 7. 이번 세션 커밋 (8/12 2차)

```
50be1b5 fix(pdf): give Simplified Chinese a font that has its characters
1d54c2c chore(pdf): drop six font files the renderer never opens
e5068ae fix(pdf): the Thai report was printing every digit and letter as a blank box
8eb3fea feat(pdf): explain the pillar characters to readers who do not read them
ad9b03c fix(reports): stamp the cover date in the reader's timezone, not UTC
ff7ec01 fix(reports): anchor the child's temperament to the day stem, preview included
```

---

## 8. 다음 세션에서 주의할 것

직전 문서 §8의 항목은 그대로 유효하다. 오늘 추가된 것:

1. **로컬이 맞아도 라이브는 다를 수 있다 — 배포 산출물을 열어 볼 것.**
   `serverless.yml`의 `!assets/fonts/*.otf` 한 줄이 폰트를 조용히 빼고, 코드는 폴백이
   있어 에러도 내지 않는다. `unzip -l .serverless/*.zip | grep fonts`로 확인하라.
   자산을 추가했으면 **프로덕션에서 실제 산출물을 받아 봐야 검증이 끝난 것이다.**

2. **`pdftotext`는 렌더링이 아니다.** 글리프가 없어도 텍스트는 추출되거나 조용히
   빠진다. 폰트 문제는 반드시 `pdftoppm`으로 이미지를 만들어 눈으로 볼 것.

3. **언어를 추가하거나 로케일을 열기 전에 글리프 커버리지부터 재라.** 30초면 끝난다.
   ```
   fontkit.openSync(path).hasGlyphForCodePoint('时'.codePointAt(0))
   ```
   태국어와 중국어 결함 둘 다 이 한 줄로 확정됐다. ja·es 리포트를 아무리 읽어도
   안 나온다.

4. **Lambda 패키지 한도(zip 50MB)가 실질적 제약이다.** 폰트 두 벌이 들어간 지금 43.7MB.
   자산을 더 넣기 전에 `npx serverless package`로 먼저 재라. `node_modules`의
   소스맵·`.d.ts`·README는 이미 제외돼 있다.

5. **`assets/fonts`는 gitignore다.** 폰트를 추가하면 `git add -f`를 잊지 말 것. 안 하면
   다음 클론에서 배포가 조용히 깨진다.

6. **프로덕션 DB 삭제는 하네스가 막는다.** 에이전트가 정리하게 하지 말고 사람이
   실행하는 편이 빠르다. 스크립트는 `output/db-backups/cleanup-test-rows.js`에 있고
   백업 없이는 삭제하지 않도록 만들어 두었다.

7. **`sandbox-serve.sh`는 끝나지 않는다.** 마지막 줄이 `exec python3 -m http.server`라
   백그라운드 태스크가 영원히 "실행 중"으로 보인다. 빌드가 끝난 것과 태스크가 끝난 것은
   다르다. `lsof -ti tcp:8080 | xargs kill`로 정리.

8. **무엇을 고칠지 정하기 전에 DB를 먼저 보라.** 실사용 리포트가 ko 12 / ja 6 / en 6이고
   결제가 5건이라는 사실이, 오늘 "다음에 무엇을 할까"의 답을 전부 바꿨다.

---

## 9. 이어서 한 일 (8/12 3차)

직전 §6.3에 "기록만 해 두는 것"으로 미뤄 둔 결함 F·G를 처리했다.

### 9.1 결함 G — 점 표기 날짜 (`2c0a09d`)

프론트가 저장하는 `2017.06.14`가 **네 곳**으로 흘러가고 있었다. PDF 표지(legacy 경로),
리포트 이메일, 결과 화면 히어로, 입력 5단계 요약. 열 개 언어 전부가 한국식 점 표기를 봤다.

저장 형식은 그대로 두고(백엔드 파서가 이 형식을 받는다) 그릴 때만 옮긴다.
`backend/src/utils/report-date.js`의 `formatBirthDate`와
`frontend/app/lib/date-display.ts`의 `formatBirthDateForDisplay`가 **같은 로케일 표**를 쓴다 —
PDF와 화면이 다른 날짜를 보이면 안 된다.

태국어는 `th-TH-u-ca-gregory`로 고정했다. th 로케일의 기본 달력은 불기라서 그냥 두면
표지에 2560년이 찍히고 리포트 안의 대운·세운 연도와 어긋난다.

**검증**: 열 개 언어의 포맷 결과를 `hasGlyphForCodePoint`로 전부 통과 확인한 뒤,
태국어·프랑스어 PDF를 실제로 렌더해 `pdftoppm`으로 눈으로 봤다. 이메일 HTML도 세 언어로
직접 렌더해 날짜 셀을 확인했다. 신규 테스트 9건(`tests/report-date.test.js`).

### 9.2 결함 F — 비활성 버튼 사유 (`b508b2d`)

막힌 단계에서 비어 있는 필수 항목을 이름으로 나열한다. 기존 라벨을 재사용해서 새 문자열은
언어당 한 줄(`missingRequired`)뿐이다.

힌트는 버튼 **행 바깥**에 둔다. 처음에 행 안에 넣었더니 5단계에서 "Start Analysis"가
두 줄로 찌그러졌다. 그리고 렌더 안에서 컴포넌트를 정의하면 안 된다
(`react-hooks/static-components`) — 조각을 돌려주는 함수로 바꿨다.

### 9.3 운영

- 백엔드·프론트엔드 배포 완료. `verify:live` **14/14**, 백엔드 테스트 **184건**.
- somyung.cc에서 입력 마법사 5단계를 실제로 통과해 확인: 요약이 `June 14, 2017`,
  마지막 단계 힌트가 `Still needed to continue: Mother's Birth Time, …`.
- **테스트 행 3건은 이미 없었다.** 드라이런 결과 `readings: 24 total, 0 test`.
  누군가 이미 지웠다(백업 파일은 `output/db-backups/`에 남아 있다).
- `frontend/.env.cloudflare` 삭제. 저장소 어디서도 읽지 않고, Pages 배포는
  `wrangler login` OAuth 세션을 쓴다.
- 부모 나이 하한 20세는 **의도된 값으로 확정**(사용자 판단). 변경 없음.

### 9.4 새로 관측한 것

- **태국어 legacy 표지에서 `รายงานพรีเมียม`가 `SoMyung` 로고와 세로로 겹친다.**
  태국어 글자 높이 때문이고, 이번 변경과 무관한 기존 문제다. legacy 경로(fallback ~6%)에서만 보인다.
- **`SoMyung_컴플라이언스_그로스_전략.md` §3.1은 이미 낡았다.** 영문 `/about/`을 라이브에서
  읽어 보면 전문이 영어다(한국어는 자격증명 `명리심리상담사 1급` 같은 용어 병기뿐).
  Week 1 목록을 실행하기 전에 각 항목을 **라이브에서 먼저 확인할 것.**
- §4.1의 503은 그대로다. 프론트를 Function URL로 바꾸는 것은 해법이 아니다 —
  `frontend/.env.production`의 주석대로 그 URL은 API Gateway의 쓰로틀링 밖에 있다.
  진짜 해법은 생성 요청을 202로 즉시 돌려주고 이미 있는 claim key 폴링에 맡기는 것이다.

---

## 10. 503 제거 (8/12 4차, `c8d2828`)

### 10.1 무엇이었나

리포트 한 건은 40~50초가 걸리는데 앞단 **API Gateway는 30초에서 끊는다**(HTTP API의
고정 한도. 늘릴 수 없다). 그래서 정상적으로 성공하는 리포트조차 클라이언트에게는
503으로 보였다. §4.1에 "사용자 피해는 없다"고 적어 두었지만, **결제한 사람이 에러
응답을 한 번 받는 경로**였고 폴링이 실패하면 그대로 실패였다.

### 10.2 왜 Function URL이 답이 아닌가

Function URL은 게이트웨이 밖이라 30초 한도가 없지만, `frontend/.env.production`의
주석대로 **API Gateway의 쓰로틀링(20 burst / 10 rps)도 함께 벗어난다.** 유료
엔드포인트 앞의 레이트 리밋을 없애는 대가로 503을 지우는 셈이라 쓰지 않았다.

### 10.3 한 것

요청 핸들러는 검증만 끝내고 **같은 Lambda를 비동기(Event)로 호출한 뒤 202**를 준다.
워커 호출은 게이트웨이 뒤가 아니므로 60초를 다 쓴다. 클라이언트는 이미 있던
`GET /saju/reading-check?claim=`으로 결과를 받아 간다.

**"202를 먼저 보내고 같은 호출 안에서 계속 만들기"는 쓸 수 없다.** serverless-http가
응답을 끝내는 순간 핸들러 프로미스가 resolve되고 Lambda는 컨테이너를 얼린다. 남은
작업은 다음 호출에 재개되거나 사라진다 — 결제 경로에 쓸 수 있는 방식이 아니다.

안전장치:

| 위험 | 장치 |
|---|---|
| 디스패치 실패(권한·SDK·throttle) | 그 자리에서 동기 생성으로 폴백 — 최악이어도 오늘과 같다 |
| 비동기 호출 재시도로 중복 생성 | `maximumRetryAttempts: 0` + 워커의 claim key 중복 검사 |
| 결제 문제를 잡 안에서 발견 | `verifyPaymentForReading`를 요청 안에서 먼저 호출 (202 뒤에 실패하면 사용자는 이유도 모르고 폴링만 한다) |
| 캐시된 옛 번들이 202를 완성본으로 오해 | `async: true`를 보내는 클라이언트에게만 새 경로를 준다 |
| 되돌리기 | `SAJU_ASYNC_REPORTS=0` — 재배포 없이 예전 동작 |

### 10.4 라이브 실측

프로덕션에 프로모 리포트를 실제로 생성해 확인했다.

```
POST /saju/calculate-promo (async:true)  → HTTP 202  4.0초
reading-check 폴링 3회(약 15초)          → status=complete, presentation=ready
CloudWatch: 요청 호출 2.5초 / 워커 호출 25.5초
```

워커가 **25.5초**를 썼다. 예전 같으면 이 요청은 게이트웨이 30초 안에 들어왔겠지만,
AI가 느린 날에는 그대로 503이 되던 구간이다. 테스트 행·프로모 사용은 정리했다
(readings 24 / promo_usage 10 — 시작 상태와 동일).

`verify:live` 14/14, 백엔드 테스트 **201건**(잡 단위 7 + 라우트 계약 10 추가).

### 10.5 이번에 확인한 다른 것들

- **레이트리밋은 응답이 빨라져도 그대로다.** `sajuPremiumLimiter`가 IP당 시간당
  20건으로 막는다 — 응답이 45초에서 2초로 줄었다고 더 많이 만들 수 있는 게 아니다.
- **"무료 프리뷰는 저장하지 않는다"는 문구는 사실이다.** `insert`는 유료·프로모
  생성 경로에만 있다(saju.service.js 390/403/415).
- **패키지 크기**: zip 45.4MB, 압축 해제 95MB. `@aws-sdk`만 32MB인데 Lambda 런타임이
  이미 SDK v3를 제공하므로, 헤드룸이 필요해지면 패키지에서 빼도 된다(코드에 폴백이
  있어 안전하다).
- **`/saju/reading-check`가 `select('*')`로 행 전체를 돌려준다** — `delivery_email`,
  `consent`(수집 시 IP 포함), `claim_key_hash`, `payment_id`까지 브라우저로 간다.
  claim key 소유자(=만든 본인)에게만 가므로 유출은 아니지만, 열쇠의 해시를 되돌려 줄
  이유는 없다. 컬럼을 명시하는 편이 낫다.
- **프리뷰 표제와 AI 본문이 서로 다른 오행을 말한다.** §10.6.

### 10.6 새로 찾은 결함 — 프리뷰가 자기 자신과 어긋난다

라이브 프리뷰(en, 2017-06-14 14:30)에서:

- 표제 상자: **`Water (水) Temperament`** — 일간 壬 기준. §2.1에서 고친 그대로 맞다.
- 바로 아래 AI 본문: **"this child's core temperament is strongly 'fire-forward'
  (many fire elements)"** — 火 4개, 개수 기준.

같은 화면에서 아이의 기질을 두 가지로 말하고 있다. `ff7ec01`은 **표제**를 일간으로
옮겼지만, AI에게는 여전히 "가장 많은 오행"을 core temperament로 앞세울 여지가 남아
있다. 유료 리포트가 아니라 **전환을 담당하는 무료 프리뷰**에서 벌어지는 일이라
체감 비용이 크다. 프롬프트 수정이 필요하고, 고치면 §2.1처럼 측정해서 확인해야 한다.
