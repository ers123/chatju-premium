name: somyung-saju
description: Korean Saju (Four Pillars of Destiny) child temperament analysis — calculates element balance, personality traits, learning styles, and parenting tips from birth data
icon: yin_yang
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'node',
      args: ['mcp-server/dist/index.js']
    })
