# 비한국어 리포트의 명리 근거 마모 — 측정과 실패한 개입 2건

작성: 2026-08-11
대상: `progress_2026-08-11.md` §6 "비한국어판 명리 근거 마모"
산출물: `output/localization/` (리포트 원문·지식 블록·심판 판정 JSON)
도구: `backend/scripts/measure-localization.js`, `backend/scripts/compare-localization.js`

---

## 1. 결론 먼저

- **문제는 실재하고 재현된다.** 한국어 리포트가 소스의 근거를 더 많이 담는다.
- **시도한 개입 2건은 모두 효과가 없었다.** 하나는 완전 무효, 하나는 소폭 악화 + 레이아웃 폴백.
- **현재 측정 도구의 잡음이 잡으려는 효과보다 크다.** 이걸 먼저 고치지 않으면
  이후의 모든 개입 판정은 잡음을 쫓는 일이 된다.
- 두 개입은 코드에 남아 있으나 **기본값 꺼짐**(`SAJU_LOCALIZED_VOICE=1`로만 켜짐).
  프로덕션은 측정된 기존 동작과 바이트 단위로 동일하다.

---

## 2. 측정 방법

고정 원국(2018-05-14 09:30, 여아, 병 일간·여름생·겁재 과다·재성/인성 부재) 하나로
언어만 바꿔 리포트를 생성하고, LLM 심판이 **소스 지식 블록의 각 신호가 리포트에
구체적으로 살아있는지**를 채점한다.

"구체(concrete)"는 부모가 오늘 당장 실행할 수 있는 서술, "일반화(generic)"는 어떤
아이에게나 들어맞을 문구, "누락(absent)"은 반영 없음.

### 2.1 처음 쓴 방법은 틀렸다

`measure-localization.js`는 리포트마다 심판을 따로 호출한다. 그랬더니 심판이 매번
소스를 다르게 쪼갰고(21개 vs 27개 신호), 결과가 **ko 14% vs fr 44%** 로 나와
§6과 정반대로 읽혔다.

교란을 제거하려고 `compare-localization.js`를 만들었다. 심판을 **한 번만** 호출해
소스에서 신호 목록을 먼저 뽑게 하고, 그 **동일 목록**으로 두 리포트를 채점한다.
그러자 방향이 뒤집혔다.

> 교훈: 리포트별 절대 점수는 비교에 쓸 수 없다. 항상 동일 호출·동일 신호 목록으로 비교할 것.

---

## 3. 측정 결과

### 3.1 문제 확인 (한국어 vs 프랑스어)

| 비교 | 한국어 | 프랑스어 | 격차 |
|---|---|---|---|
| ko vs baseline-fr | 75% (9/1/2) | 50% (6/2/4) | 25pt |
| ko vs cultural-fr | 100% (11/0/0) | 64% (7/3/1) | 36pt |

절대값은 흔들리지만 **격차의 방향과 크기는 두 번 모두 재현**됐다. §6은 사실이다.

### 3.2 손실이 집중된 지점

두 심판이 독립적으로 같은 것을 지목했다:

> "피해야 할 말 3종·효과 말 3종, 정지 신호, 비교 전환, 전환 타이머, 기록/도움 확인"

즉 **주제는 살아남고 문구 수준 대사가 죽는다.** 프랑스어 리포트에도
"Mots à éviter / Mots efficaces" 슬롯은 채워져 있지만, 이 아이의 신호에서 유도된
대사가 아니라 일반 육아 조언으로 채워진다.

이는 progress §6이 "정보가 아니라 대본을 파는 형태"라고 한 바로 그 대본 레이어다.

---

## 4. 실패한 개입 2건

### 4.1 지시문 재구성 + 언어별 문화 레지스터 — 효과 0

기존 지시문이 `Translate every idea here into French`였다. 번역을 시키니 번역체이거나,
옮기기 어려운 대사는 통째로 일반론으로 대체된다고 보고, 지시를 "번역"에서
"기능 이식"으로 바꿨다. 여기에 언어별 양육 화법 좌표(`cultural-register.js`)를 덧붙였다.

**결과: 동일 심판 비교에서 10/2/1 대 10/2/1 — 신호별로 완전히 동일.**
심판은 오히려 baseline을 선호했다("mots à éviter 세 문구를 더 충실히 재현").

progress §9.2가 경고한 그대로다 — 프롬프트 지시로 형식·문구를 강제하는 레버는 이
파이프라인에서 작동하지 않는다(이번이 5번째 실패).

### 4.2 프랑스어 대사 저작본 주입 — 소폭 악화 + 폴백

§9.2가 가리키는 방향(지시 대신 데이터)에 따라, 오행별 "피할 말 → 대신할 말" 15쌍을
**번역이 아니라 프랑스어로 저작**해 데이터로 주입했다(`harmful-phrases-i18n.js`).
예: 「형은 안 그랬는데」 → « Ton frère, lui, il n'en faisait pas toute une histoire. »

**결과: 90% → 80%** (레지스터만 켠 대조군 대비). 게다가 그 실행에서
`presentationStatus=fallback`이 났다 — 유료 리포트가 편집 레이아웃을 잃는 실패 모드다.

n=1이라 폴백의 인과는 단정할 수 없다. 다만 이득이 확인되지 않은 변경을 유료 경로에
켜 둘 이유는 없다.

---

## 4.5 계측기 재건 후 다시 잰 결과 (추가: 같은 날 후속 작업)

아래 §5에서 지목한 대로 계측기부터 고쳤다. `scripts/eval-localization.js`가
①신호 목록을 `buildKnowledgeContext().signals`에서 결정론적으로 받고
②같은 리포트를 3회 채점해 신호별 다수결·점수 중앙값을 쓰고
③원국을 여러 개 돌리고
④언어별 루브릭(`localization-rubric.js`)으로 합격/불합격을 낸다.

**심판 자기일관성이 88~92%로 올라왔다.** 기존 ±25pt 요동이 ±7pt 수준으로 줄었다.

그리고 신호에 종류를 붙이자(`kind: 'script' | 'analytic'`) 그림이 선명해졌다.

| lang | 전체 | **script** | analytic | 일관성 |
|---|---|---|---|---|
| ko | 22% | **75%** | 5% | 91% |
| fr | 19% | **54%** | 7% | 90% |

- **§6은 실재하지만 대사(script) 레이어에 국한된 현상이다.** 여기서만 21pt 격차가 난다.
- **analytic 신호는 ko 5% / fr 7%로 언어와 무관하게 바닥이다.** 이건 결함이 아니라
  설계다 — 프롬프트가 "십성 분포 같은 용어를 그대로 나열하지 말라"고 금지한다.
- 따라서 §3의 "전체 보존율" 수치(75% vs 50% 등)는 신호 단위가 거칠어 과대평가된
  값이었다. 방향은 맞았지만 크기는 이 표를 믿어야 한다.
- 게이트 임계값은 그래서 `groundingScript` 축에 건다. ko 0.65 / 그 외 0.6.

부수 발견: **한국어 리포트도 nativeness 3/5로 걸린다.** 번역 문제가 아니라 리포트
전체가 보고서체로 읽힌다는 뜻이므로, 언어와 무관한 별도 과제다.

---

## 5. (당시 판단) 진짜 병목: 측정 도구

같은 산출물이 비교마다 다르게 나온다.

| 산출물 | 측정된 값들 |
|---|---|
| baseline-fr | 50%, 77% |
| cultural-fr | 64%, 90% |

**잡음 폭이 ±25pt로, 잡으려는 효과 크기보다 크다.** 이 상태에서 "90% vs 80%"는
악화의 증거가 못 되고, 마찬가지로 어떤 개선도 확인할 수 없다.

원인은 심판이 매 호출마다 소스를 다르게 쪼개는 것(신호 10~13개로 요동).

### 다음에 할 일 (개입보다 먼저) — **완료됨, §4.5 참고**

1. ~~신호 목록을 고정한다~~ → `buildKnowledgeContext().signals`로 결정론화
2. ~~반복 채점 + 다수결~~ → `--rounds=3`, 신호별 다수결·점수 중앙값
3. ~~원국을 늘린다~~ → `--charts=N` (기본 5개 정의됨)

이제 §4의 개입 2건을 이 계측기로 다시 판정할 수 있다:

```bash
npm run eval:localization -- --langs=fr --charts=3 --rounds=3 --label=voice-off
SAJU_LOCALIZED_VOICE=1 node scripts/eval-localization.js --langs=fr --charts=3 --rounds=3 --label=voice-on --regenerate
```

`groundingScript` 축에서 voice-on이 voice-off를 유의미하게 넘으면 그때 켠다.
잔여 잡음이 ±7pt이므로 그보다 작은 차이는 여전히 판정하지 말 것.

---

## 6. 코드 상태

| 파일 | 상태 |
|---|---|
| `scripts/eval-localization.js` | **현행 계측기** (`npm run eval:localization`) |
| `data/saju-knowledge/localization-rubric.js` | 언어별 합격 기준. 지금은 평가용, 이후 출력 게이트로 승격 가능 |
| ~~`scripts/measure-localization.js`~~ | 삭제 — 리포트별 개별 채점이라 비교 불가 |
| ~~`scripts/compare-localization.js`~~ | 삭제 — eval이 결정론적 신호 목록으로 대체 |
| `data/saju-knowledge/cultural-register.js` | 신규, **기본 꺼짐** |
| `data/saju-knowledge/harmful-phrases-i18n.js` | 신규(프랑스어 15쌍), **기본 꺼짐** |
| `services/saju-knowledge.js` | 플래그 분기 추가. 꺼짐일 때 기존 동작과 바이트 동일 |
| `services/saju.service.js` | `generateAIInterpretation`를 측정용으로 export |

켜는 법: `SAJU_LOCALIZED_VOICE=1`

### 남은 관찰 (미수정)

한국어 경로에서 따옴표가 이중으로 나온다 — `피할 말 ""좀 조용히 해.""`.
소스 데이터에 이미 따옴표가 있는데 템플릿이 한 번 더 감싼다. 다만 이 경로가 현재
75~100%로 가장 잘 나오는 경로라, 측정 없이 건드리지 않았다.
