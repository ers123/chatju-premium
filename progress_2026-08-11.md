# SoMyung Progress — 2026-08-11

작성 기준: 2026-08-11 KST
브랜치: `codex/somyung-launch-ready` (= PR #10)
직전 문서: `progress_2026-08-04.md` (§13, §14, §15는 8/8~8/10 작업 기록)

---

## 1. 한 줄 요약

**실제 유료 결제(PayPal 샌드박스)를 처음으로 끝까지 돌렸고, 그 과정에서 자동 검증이 잡지
못한 결함 8건을 찾아 전부 수정·배포했다.** 결제 파이프라인은 정상 작동하며, 남은 것은
언어별 사람 눈 QA와 가격 결정이다.

---

## 2. 이번 세션에서 확정된 사실

### 2.1 결제 흐름은 실제로 작동한다

PayPal 샌드박스로 2회 결제 완료. 로그·DB로 교차 검증한 결과:

| 단계 | 결과 |
|---|---|
| PayPal create / capture | 성공 |
| `payments.status` | `completed` ($4.99 USD) |
| 결제 검증 → 리포트 생성 | 성공 (2회 호출, 약 2만자) |
| 명리 지식 주입 | 작동 |
| `presentationStatus` | 1차 fallback → **수정 후 ready (9섹션)** |
| DB 저장 / 이메일 | 성공 / `sent` |
| 언어 일치 | 한글 0자 |

### 2.2 테스트 환경 (재현 방법)

- 백엔드: `backend/.env.sandbox-test` (PayPal만 sandbox, DB·AI는 프로덕션) → `node index.js`, 포트 3001
- 프론트: **`frontend/scripts/sandbox-serve.sh`** 한 방으로 빌드 + 서빙 (포트 8080)
- 포트 8080은 임의가 아니다. 백엔드 CORS 허용 목록이 코드에 고정돼 있고 3000은 없다.
- 샌드박스 구매자 계정: developer.paypal.com → Testing Tools → Sandbox Accounts

> ⚠️ 샌드박스 빌드와 프로덕션 빌드가 같은 `frontend/out`을 쓴다. 손으로 빌드하면 서로
> 덮어쓴다. 이 사고로 테스트가 두 번 헛돌았다(로컬이 프로덕션 API를 호출해 CORS 차단,
> 수정 전 번들이 계속 서빙돼 고친 것이 안 고쳐진 것처럼 보임). **반드시 스크립트로 띄울 것.**

---

## 3. 수정한 결함 8건

모두 **사장님이 실제로 클릭해서** 발견됐다. 자동 검증은 하나도 잡지 못했다.

| # | 결함 | 원인 | 커밋 |
|---|---|---|---|
| 1 | 영어 UI에 한국어 본문 | 언어 컨텍스트가 `useState('ko')`로 시작하고 `useEffect`에서야 확정되는데, 결과 페이지가 그 전에 API 호출 | `9105196` |
| 2 | 생년월일이 `0019.06.30` | `type="date"`가 두 자리 연도를 서기 19년으로 저장. 검증이 상한만 확인 | `9105196` |
| 3 | 쿠키 배너 레이아웃 붕괴 | `globals.css`의 레이어 밖 `* { padding: 0 }`이 Tailwind 여백 유틸리티를 전부 무력화 | `65d92b3` |
| 4 | 한도 초과인데 "네트워크를 확인하세요" | 에러 카드가 사유와 무관하게 네트워크 안내를 항상 덧붙임 | `bb849a8` |
| 5 | **유료 리포트가 fallback으로 강등** | 유료 payload에 `subjectName` 누락 → 표지 `child` 비어 계약 검증 실패 | `7e07f12` |
| 6 | PDF 액자가 한국어 | 렌더러의 제목·설명문·"계속"이 코드에 한국어로 박혀 있음 (21개 문자열) | `9ef730f` |
| 7 | 사주 표에 `목 + 토` | 계산기의 한국어 오행명을 구조화 경로가 변환하지 않음 | `9ef730f` |
| 8 | 프랑스어 따옴표 혼용 | 모델이 « » 와 " " 를 섞어 씀 | `8d05f05` |

### 3.1 특히 중요한 것: #5

**유료 결제를 한 번도 안 해봤으면 영영 못 찾았을 버그다.** 프로모 경로에는 `subjectName`이
처음부터 있어서, 지금까지의 모든 promo 기반 검증은 통과했다. 결제 경로에만 빠져 있었다.

이름이 `null`이면 기본값이 적용되지 않아(기본값은 `undefined`에만 작동) 표지가 비고,
계약 검증에 걸려 **리포트 전체가 강등**됐다. 양쪽 다 고쳤다 — 프론트는 이름을 보내고,
백엔드는 `null`/공백도 언어별 기본 호칭(`Your child`, `お子さま` …)으로 대체한다.

### 3.2 함께 발견한 사실 오류

프랑스어 리포트에 `Mois 8 — 정유 (丙申) / 丁酉`. 한글 간지가 나왔고, `정유`는 `丁酉`인데
`丙申`(다른 간지)이라고 적었다. **차트에 대한 사실 오류.** 원인은 프롬프트가 모델에게
한국어 간지를 주고 한자 변환을 시킨 것. 계산기가 이미 `pillar.hanja`를 갖고 있어서
비한국어에는 한자를 직접 주도록 바꿨다(`f97e415`).

---

## 4. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포 |
| 프론트엔드 | 최신 배포 (Cloudflare Pages) |
| DB 마이그레이션 001~007 | 전부 적용 (007은 8/10 실행 완료) |
| 리포트 1차 통과율 | 10/10 (재시도 끈 상태 측정) |
| 비한국어 한글 혼입 | PDF 실측 0건 (en·ja·zh·vi·id·es·pt·fr·th) |
| 백엔드 테스트 | 140건 통과 |

### 4.1 프론트엔드 배포 방법 (중요)

Cloudflare Pages 프로젝트는 **Git에 연결돼 있지 않다.** 푸시해도 배포되지 않는다.
대시보드의 `somyung`이라는 Source 표시는 업로드 시 붙인 라벨이지 git 브랜치가 아니다.

```bash
cd frontend
npm run build     # next.config.ts가 output:'export' → out/
npx wrangler pages deploy out --project-name=somyung --branch=somyung
```

`NEXT_PUBLIC_API_URL`은 반드시 **API Gateway**여야 한다
(`https://0eo64hyuv7.execute-api.ap-northeast-2.amazonaws.com`). Lambda Function URL은 같은
Lambda에 닿지만 API Gateway 밖이라 **쓰로틀링이 사라진다.** `frontend/.env.production`이
그쪽을 가리키고 있었고 8/10에 고쳤다. 자세한 내용은 `DEPLOYMENT.md`.

---

## 5. 나이 제한 (질문 답변)

| 대상 | 범위 | 의미 |
|---|---|---|
| 아이 | 오늘 ~ 18년 전 | 만 0~18세 (미성년) |
| 부모 | 1945-01-01 ~ 2005-12-31 | 만 20~81세 |

성인 자녀가 고령 부모와의 관계를 보는 조합은 **구조적으로 불가능**하다(아이가 18세 이하로
강제됨). 양육 가이드 제품이므로 일관되지만, 부모 하한 20세는 다소 임의적이고, 성인 자녀–부모
시장을 막고 있다는 점은 의도된 선택인지 확인이 필요하다.

---

## 6. 리포트 품질 — 제3자 관점 (프랑스어 기준)

**언어 품질은 기대 이상.** 기계번역체가 아니다.
`« Qu'est-ce que tu chipotes ? »` 같은 실제 구어가 나오고, 신금(辛金)을
`un petit bijou bien taillé`로 옮기는 등 명리 용어를 노출하지 않고 은유만 살린다.
섹션 제목도 전부 프랑스어(`L'enfant en un coup d'œil`, `Forces cachées`).

**구조도 팔 만하다.** 오해 → 실제 → 더 나은 표현의 3단이 일관되고, 부모가 그대로 읽어서 쓸
수 있는 대사가 들어 있다. "정보"가 아니라 "대본"을 파는 형태.

**남은 아쉬움 (미해결)**: 명리 근거가 비한국어판에서 덜 드러난다. 지식은 한국어로 주입되고
출력만 번역되다 보니, 한국어판의 도화·편관 같은 구체적 신호가 프랑스어판에서는
"섬세하다/기준이 높다" 수준으로 마모되는 경향이 있다. 구조적 문제라 시간이 걸린다.

---

## 7. 남은 일

### 7.1 가격 결정 전에 할 것

$9.99 인상을 검토 중. 내용만 보면 가능하다고 보지만, **오늘 하루에 표면 결함이 8건 나왔고
전부 사람이 클릭해서 발견됐다.** 자동 검증은 프론트엔드가 잘못된 값을 보내는 경로를
지나가지 않기 때문에 이 계열을 구조적으로 못 잡는다.

→ **언어별로 실제 결제를 한 번씩 돌려보고, 아무것도 안 나오면 그때 올릴 것.**
   특히 일본어·스페인어는 아직 사람 눈으로 안 봤다.

### 7.2 기술 과제

- 비한국어판 명리 근거 마모 (§6) — 지식 모듈을 언어별로 번역할지 검토
- 무료 미리보기 레이트 리밋 **IP당 10회/시간**이 적절한지 결정.
  한국 이동통신사 NAT 환경에서는 첫 방문 고객이 막힐 수 있다. 유료 퍼널 맨 앞이라 타격이
  직접적. 30회/시간 정도로 올리는 것을 권함(트래픽이 적어 비용 리스크가 작음).
- `frontend/.env.cloudflare`의 Cloudflare API 토큰이 만료됨. 안 쓰면 파일 삭제 권장.
  (wrangler가 OAuth로 로그인돼 있어 배포에는 불필요)

### 7.3 운영

- 테스트로 생긴 `payments`/`readings` 행 3건(샌드박스 주문) 정리 여부 결정
- staging Supabase는 삭제된 상태. 필요하면 새로 만들고 URL 교체 (keepalive 워크플로는 8/10에 제거)

---

## 8. 이번 세션 커밋 (8/11)

```
8d05f05 fix(reports): use French quotation marks consistently
f97e415 fix(reports): give the model hanja pillars instead of asking it to convert
9ef730f fix(pdf): translate the report chrome, and stop printing Korean element names
3251957 test: one command to rebuild and serve the local sandbox site
7e07f12 fix(reports): a missing nickname must not downgrade a paid report
bb849a8 fix(ui): stop telling rate-limited visitors to check their network
9105196 fix(i18n): send the visitor's language, and reject impossible birth years
65d92b3 fix(ui): layer the global reset so Tailwind spacing utilities apply
d9d928b docs: correct the frontend deploy instructions
```

---

## 9. 다음 세션에서 주의할 것

1. **로컬 테스트는 `frontend/scripts/sandbox-serve.sh`로만 띄운다.** 손으로 빌드하면
   프로덕션 번들과 섞인다.
2. **프롬프트 지시로 형식을 강제하려 하지 말 것.** 오늘 라벨·섹션 제목·간지·따옴표 네 번
   시도해서 네 번 실패했다. 파서를 관용화하거나 생성 후 결정론적으로 정규화하는 쪽이 맞다.
3. **`npm test`는 `grep "^Tests:"`로 확인한다.** `tail -3`은 통과/실패 카운트를 안 보여줘서
   실패한 상태로 커밋·배포한 적이 있다.
4. **재시도 경로를 잊지 말 것.** `interpretationText`를 통째로 교체하므로, 1차에만 적용한
   후처리는 재시도가 일어나면 조용히 사라진다(따옴표 수정이 이것 때문에 한 번 실패).
5. 저장된 리포트는 **생성 시점 언어가 박혀 있다.** 렌더 언어만 바꿔서는 번역되지 않는다.
   다른 언어를 검토하려면 그 언어로 새로 생성해야 한다.
