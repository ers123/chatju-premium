# SoMyung Progress — 2026-08-18

`progress_2026-08-13b.md`에서 이어진다. 그 세션이 며칠에 걸쳐 이어졌고, 이 문서는
**공유 루프 구축과 그 과정에서 드러난 삭제 결함**을 다룬다.

> 날짜 주의: 8/13~8/14 작업분 문서(`progress_2026-08-13b.md`, `docs/DPIA_2026-08-13.md`,
> `docs/privacy-minor-data-review_2026-08-13.md`)는 작성 시점 날짜를 달고 있다.
> 이 문서부터가 8/18이다.

---

## 1. 한 줄 요약

**팔로워 0을 우회하는 배포 경로를 만들었고, 만들다가 삭제가 안 되는 리포트 19건을
발견했다.** 공유 링크는 라이브에서 5개 계약(OG 카드·이름 무유출·멱등·인증·철회)을
전부 통과했다. 그리고 프로모 리포트가 외래키 때문에 **삭제 자체가 거부**되고 있었다 —
어제 자동화한 보존기간 청소가 조용히 실패할 자리였다.

---

## 2. 소셜 자산 실사 — "계정 없음"은 틀렸다

맥미니 `~/Projects/nova-digital`(NOVA Digital = SoMyung 마케팅 조직)에 4월부터 있었다.

| 채널 | 상태 (2026-08-18 실측) |
|---|---|
| **Threads** `@sungha009` | 토큰 **유효** — launchd가 매일 자동 갱신. **팔로워 0명**, 마지막 게시물 2025-09-19 |
| Instagram `@sungha009` | Professional/Creator, FB페이지 연결, 게시물 50. **토큰 2026-06-13 만료**(갱신 잡 없음) |
| X | 토큰 존재, 미검증. 원래 "매출 $100/월 후" 조건 |

**4월 14일 이후 모든 마케팅이 멈춘 이유**가 `progress/2026-04-14.md`에 있었다:
> Paused: 개인정보 저장 이슈 점검 중 — 모든 에이전트 활동 중단

**그 사유가 8/13 작업(10개 시장 조사 + DPIA + 갭 2건 수정)으로 이미 해소됐다.**
차단은 풀렸고, 남은 문제는 다른 것이었다 — 팔로워가 0이라는 것.

### 전략 판단: 콘텐츠가 아니라 배포 경로가 부족했다

준비된 콘텐츠 180건을 팔로워 0 계정에 올리면 0명이 본다. 1년 휴면 + 참여 이력 0인
콜드 계정은 비팔로워 도달 배분에서도 뒤로 밀린다. 그래서 순서를 바꿨다:

1. **제품 자체의 공유 루프** ← 팔로워가 필요 없는 유일한 경로. 이번에 구현
2. SEO/GEO — 블로그 18편·GEO 75·MCP 공개. 검색은 팔로워를 안 본다
3. Threads는 "게시"가 아니라 **댓글 참여**로 시작(사람이 해야 하는 일, 하루 15분)

**하지 않기로 한 것:** 180건 일괄 예약 게시(0명에게 30일치를 뿌리면 계정이 봇으로
학습된다), Instagram 즉시 재개(팔로워 0에서 IG가 가장 불리하고 영상 제작 부담이 크다).
IG는 당분간 **공유 루프의 착륙지**로만 쓴다.

---

## 3. 공유 루프 — 구현과 검증

### 3.1 왜 정적 페이지로는 안 되는가

카카오톡·Threads가 링크 미리보기를 만들려면 그 URL을 긁을 때 서버가 **공유 건마다 다른**
`og:title`을 돌려줘야 한다. 프론트는 `output: 'export'`(빌드 시점에 HTML을 다 구움)라
아직 없는 토큰의 페이지를 미리 만들 수 없다.

→ **Cloudflare Pages Function**이 `/saju/share/:token`만 가로채 엣지에서 HTML을 만든다.
나머지 사이트는 정적 그대로. 배포 로그의 `✨ Uploading Functions bundle`로 확인됨.

### 3.2 개인정보 설계 (아이 이름 없음)

- 백엔드가 **화이트리스트**로 오행 결과만 내보낸다(`share.service.js`). 이름·생년월일을
  읽는 코드 자체가 없다 — 실수로 넣을 경로를 만들지 않는 것이 최선의 방어다
- **옵트인**: 부모가 버튼을 누르기 전에는 토큰이 존재하지 않는다
- **철회 가능**: 끄면 즉시 홈으로 302. 다시 켜면 **새 토큰**(옛 링크가 되살아나면
  "껐다"는 결정이 취소되는 셈)
- 없는 링크는 404가 아니라 **홈으로** 보낸다(404는 "존재했다 사라진 링크"임을 알려준다)
- `noindex` — 얇은 페이지 수천 개가 색인되면 사이트 전체 SEO가 내려간다

### 3.3 라이브 검증 (5개 계약 전부 통과)

| 검증 | 결과 |
|---|---|
| OG 카드 | `og:title = "우리 아이는 금(金) 기질입니다"`, 이미지 200 image/png |
| **아이 이름 유출** | **0회** (`도윤` 등장 0, 생년월일 흔적 0) |
| 멱등성 | 재요청 시 `reused: true`, 같은 토큰 |
| 인증 | 소유 증명 없이 생성 시도 → **401** |
| 철회 | 끄기 → 302 → `https://somyung.cc/` |

퍼널도 `share_created` / `share_view`를 정상 계측했다. 검증 데이터는 정리했다.

---

## 4. 발견된 결함 — 프로모 리포트가 삭제되지 않는다

검증 데이터를 지우려다 나왔다:

```
update or delete on table "readings" violates foreign key constraint
"promo_usage_reading_id_fkey" on table "promo_usage"
```

`promo_usage.reading_id`가 `readings(id)`를 **ON DELETE 규칙 없이** 참조한다.
**실측 28건 중 19건(프로모 리딩 전부)이 삭제 불가 상태.**

### 왜 심각한가

1. **어제 자동화한 보존기간 청소(매월 1일)가 이 행들에서 조용히 실패한다.**
   privacy policy는 365일 삭제를 공개하고 있고, 가장 오래된 리딩이 2026-03-20이므로
   **2027-03-20에 그 약속이 깨진다.**
2. 삭제권(GDPR Art 17 / PIPA 제36조) 요청도 같은 이유로 이행 불가.
3. DPIA §3.3이 "365일 후 삭제"를 통제 수단으로 적었는데, **그 통제가 프로모 경로에서
   작동하지 않고 있었다.**
4. `promo_usage`는 `child_name`·`child_birth_date`까지 들고 있다 — 리포트를 지워도
   아이 데이터가 남는 구조다.

### 수정 (migration 011, **실행 대기**)

- FK를 `ON DELETE SET NULL`로 교체. 프로모 **사용 기록은 남긴다**(재사용 방지 근거)
- 이미 연결이 끊긴 행의 `child_name`·`child_birth_date`를 비운다
- 리포트 삭제 시 아이 데이터도 같이 비우는 트리거 추가 —
  `SET NULL`은 `reading_id`만 비우고 이름은 그대로 두기 때문

> 재사용 방지에 필요한 것은 (프로모 코드, 이메일) 쌍이지 아이 정보가 아니다.
> 애초에 저장할 이유가 없던 값이다.

---

## 5. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포. 테스트 **266건 / 19 suites** |
| 프론트엔드 | 최신 배포 + **Pages Function 라이브** |
| migration | **010까지 실행 완료**. **011 실행 대기** |
| 공유 루프 | 라이브·검증 완료 |
| 퍼널 | `share_created`/`share_view` 추가. 검증 행 정리, 0에서 실트래픽 대기 |

### 새 자산
- `backend/migrations/010_share_links.sql` (실행됨) / `011_promo_usage_erasure.sql` (대기)
- `backend/src/services/share.service.js` — 화이트리스트 요약
- `frontend/functions/saju/share/[token].ts` — 엣지 OG 렌더러
- `frontend/components/saju/ShareLinkPanel.tsx` — 옵트인·철회 UI

---

## 6. 다음

1. **migration 011 실행** ← 삭제 결함. 2027-03에 터지지만 지금 고치는 게 싸다
2. Threads 댓글 참여 시작(사람 몫, 하루 15분) — 계정이 콜드에서 벗어나야 게시가 의미
3. 프리뷰 200건 도달 시 $19.99 판정
4. 공유 루프 지표: `share_view / share_created`가 1을 넘으면 루프가 도는 것
5. EN/JA 유료 월 10건 초과 시 한국 결제 레일 재개

---

## 7. 교훈

1. **"계정이 없다"를 확인하지 않고 세션 내내 전제로 썼다.** 맥미니에 4월부터 있었고,
   멈춘 이유도 기록돼 있었다. 사람이 "없다"고 말한 것도 확인 대상이다.
2. **정리 작업이 결함을 찾는다.** 테스트 데이터를 지우다가 19건 삭제 불가를 발견했다.
   만들기만 하고 지워보지 않으면 삭제 경로는 영원히 검증되지 않는다.
3. **팔로워 0은 콘텐츠 문제가 아니라 배포 문제다.** 콘텐츠를 더 만드는 대신 배포
   경로를 만들었다.
