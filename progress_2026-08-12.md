# SoMyung Progress — 2026-08-12

작성 기준: 2026-08-12 KST
브랜치: `codex/somyung-launch-ready` (= PR #10)
직전 문서: `progress_2026-08-11.md`
상세 측정 기록: `localization_findings_2026-08-11.md` (§1~13)

---

## 1. 한 줄 요약

**`progress_2026-08-11.md` §6의 "비한국어판 명리 근거 마모"를 측정 가능한 문제로
바꾼 뒤, 9개 언어의 부모 대사와 생활 보완표를 번역이 아니라 저작으로 다시 쓰고,
근거가 확인된 6개 언어에만 적용했다.** 과정에서 계측기를 한 번 갈아엎었고,
그 전에 내린 판정 하나를 스스로 뒤집었다.

---

## 2. 이번 세션에서 확정된 사실

### 2.1 §6은 사실이지만, 대사(script) 레이어에 국한된다

주입 신호를 종류별로 나눠 재니 그림이 달라졌다.

| lang | 전체 | script | analytic |
|---|---|---|---|
| ko | 22% | 75% | 5% |
| fr | 19% | 54% | 7% |

- 언어 간 차이는 **부모가 소리 내어 읽는 대사**에서만 난다.
- analytic(십성 분포·조후 해설 같은 판단 근거)은 **한국어판을 포함해 모든 언어에서
  5% 안팎**이다. 결함이 아니라 설계다 — 프롬프트가 "용어를 그대로 나열하지 말라"고
  금지한다.

### 2.2 리포트는 번역되지 않는다 (아키텍처 확인)

"한국어로 쓰고 번역한다"는 구조가 아니다. AI 호출 2번(섹션 1–5 / 6–9 병렬)으로
**처음부터 대상 언어로 직접 생성**한다. 지시문도 비한국어는 전부 영어
(`systemMessageBaseEn`). 한국어인 것은 **주입되는 참고자료 약 5,300자**뿐이다.

### 2.3 한글 유출의 출처는 두 곳이 전부였다

비한국어 리포트 94건을 전수 집계했다. **5건(5%)** 에만 한국어 낱말이 있었고,
정확히 두 갈래였다.

| 언어 | 낱말 | 출처 |
|---|---|---|
| en | 기준, 미역, 해조류 | `elementRemedies` (색·음식·회피환경 표) |
| ja | 그냥, 넘어가, 의견이, 없어 | 「그냥 좀 넘어가」·「넌 왜 네 의견이 없어?」 대사 |

**제3의 출처는 없다.** 둘 다 이번에 처리했다.

> 그래서 지식 베이스 1,300줄을 9개 언어로 번역할 근거는 없다. 참고자료의 언어가
> 문제가 되는 것은 **그 조각이 출력에 그대로 옮겨질 때뿐**이고, 그 조각은
> 대사와 추천 항목 둘뿐이었다.

### 2.4 일본어 유료 리포트의 절반이 편집 레이아웃을 잃고 있었다

생성해 둔 리포트 86건의 `presentationStatus`를 집계한 결과다.

| 설정 | fallback |
|---|---|
| ja — 어제까지의 프로덕션 | **3/6 (50%)** |
| ja — 대사 저작본 적용 후 | **0/6** |
| 현재 설정 전체 | 5/82 (6%) |

`progress_2026-08-11.md` §3 결함 #5와 같은 실패 모드다. 대사 저작본의 이득은
근거 보존율 +19pt만이 아니었고, **일본어 리포트 절반의 레이아웃을 되찾은 쪽이
실질적으로 더 크다.**

---

## 3. 한 일

### 3.1 계측기 재건 (`scripts/eval-localization.js`)

처음 만든 측정 도구는 심판에게 소스를 주고 신호를 직접 뽑게 했다. 심판이 호출마다
10~13개로 다르게 쪼갰고, **같은 산출물이 50%와 77%로 나왔다.** 잡음이 재려는
효과보다 커서 어떤 개입도 판정할 수 없었다.

바꾼 것:

1. 신호 목록을 `buildKnowledgeContext().signals`에서 **결정론적으로 확정**. 심판은
   뽑지 않고 채점만 한다.
2. 같은 리포트를 **3회 채점**해 신호별 다수결·점수 중앙값.
3. **원국 복수**(기본 5개 정의).
4. 언어별 루브릭(`localization-rubric.js`)으로 합격/불합격 판정.
5. 생성물 캐시 — 심판만 바꿔 재실행할 때 재생성 비용 0.

**심판 자기일관성 88~92%.** `npm run eval:localization`.

### 3.2 부모 대사 9개 언어 저작

오행별 "피할 말 → 대신할 말" 15쌍을 **번역이 아니라 저작**했다
(`data/saju-knowledge/harmful-phrases-i18n.js`).

예: 「형은 안 그랬는데」
→ fr « Ton frère, lui, il n'en faisait pas toute une histoire. »
→ ja 「お兄ちゃんはこんなことなかったのに。」
→ es «Tu hermano no me hacía esto.»

`avoid`는 그 문화 부모가 실제로 무심코 내뱉는 말이어야 한다. 교과서 문장이면
부모가 자기 모습을 못 알아본다.

### 3.3 생활 보완표(색·음식·활동) 9개 언어 저작

`saju.service.js`에 한국어로 하드코딩돼 있던 표를 언어별로 다시 골랐다
(`data/saju-knowledge/element-remedies-i18n.js`). 오행 논리는 보존하되 그 부엌에
실제로 있는 것으로 바꿨다.

| 수(水) 음식 | |
|---|---|
| 원본 | 미역, 해조류, 검은콩, 두부 |
| en | sardines, black beans, tofu, lightly salted broths |
| fr | algues (nori), haricots noirs, sardines, bouillons |
| es | sardinas o boquerones, mejillones, aceitunas negras |
| th | ปลาทะเล, กุ้งและหอย, ถั่วดำ |

열대권(id·th)은 사계절 대신 우기·건기 프레임을 쓴다. 자카르타 독자에게 "봄"은
미역을 권하는 것과 같은 층위의 오류다.

오행명도 프롬프트에 한국어로 박혀 있었다(`부족 오행 (수) 보완`). 기둥을 `丁酉`로
쓰는 관례에 맞춰 `水 (Water)`로 통일했다.

### 3.4 기타

- 무료 프리뷰 레이트리밋 **10 → 30회/시간**. IP 키 기반인데 한국 통신사 NAT가 IP를
  공유해 무관한 첫 방문자끼리 할당량을 소진한다. 유료 퍼널 최상단이라 오탐 1건이
  고객 1명이다.
- `buildInterpretation()` 도입 — 재시도가 `interpretationText`를 통째로 교체하므로
  1차에만 건 후처리는 조용히 사라진다(§9.4 경고). 길이검증+정규화를 한 곳으로
  합쳐 **구조적으로 불가능**하게 만들었다.
- 튜닝 노브를 `serverless.yml`에 배선. 안 하면 Lambda까지 전달조차 안 된다.
- `output/`·`out-sandbox/`·`tmp/` gitignore (측정 산출물 3.4MB).

---

## 4. 언어별 판정 결과

**판정 규칙: 평균이 아니라 범위.** OFF 최고값 < ON 최저값일 때만 켠다.
생성 잡음이 원국당 최대 29pt까지 흔들려 평균은 쉽게 속는다.

| lang | OFF | ON | U | 이득 | 판정 |
|---|---|---|---|---|---|
| id | 30 / 27 | 61 / 52 | 0 | +29pt | **적용** |
| th | 31 / 39 | 66 / 62 | 0 | +29pt | **적용** |
| fr | 36 / 25 | 57 / 55 | 0 | +26pt | **적용** |
| en | 27 / 54 | 59 / 66 | 0 | +22pt | **적용** |
| ja | 53 / 33 | 57 / 65 | 0 | +19pt | **적용** |
| pt | 42 / 50 | 57 / 56 | 0 | +11pt | **적용** |
| zh | 58 / 43 | 57 / 56 | — | +6pt | 보류 |
| es | 56 / 48 | 62 / 51 | — | +4pt | 보류 |
| vi | 38/43/42/45 | 61/43/62/56 | 1.5 | — | 보류 |

### 4.1 이 개입의 정체는 "바닥 올리기"다

ON은 어느 언어든 **56~63%로 수렴**한다. 그래서 각 언어가 얻는 양은 거기까지 남은
거리와 정확히 비례한다. id 28% → +29pt, zh 50% → 0.

zh·es를 켜지 않는 것은 저작본이 나빠서가 아니라 **이미 그 높이에 있어 얻을 것이
없기 때문**이다. 저작본은 트리에 남겨 두었다.

### 4.2 베트남어 — 중앙값에 속지 않은 사례

중앙값 42.5 → 58.5로 켠 언어들과 구분이 안 된다. 그런데 ON 4회 중 하나가 43%,
OFF 4회 중 하나가 45%로 범위가 교차한다. 동순위 보정 Mann-Whitney U = 1.5
(α=0.05 임계 0). **미분리.**

여기서 방법론 실수 하나를 피했다 — ON을 4번 돌린 뒤 OFF 2번과 비교하려다 멈췄다.
시행이 많으면 최대값이 우연히 높게 나올 확률이 커진다. OFF도 4번으로 늘렸고,
그 추가 실행에서 45%가 나와 OFF 천장이 올라갔다. **균형을 안 맞췄으면 켰을 것이다.**

---

## 5. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포 (`2718f63`), `verify:live` 14/14 |
| 프론트엔드 | 변경 없음 (이번 작업은 전부 백엔드) |
| 대사 저작본 적용 | **en, ja, fr, pt, id, th** (6개) |
| 대사 한국어 폴백 | ko, zh, es, vi |
| 생활 보완표 | **9개 언어 전부 저작 완료** |
| 백엔드 테스트 | **175건** (11 suites) |
| 미커밋 | 0 |

### 5.1 끄는 법

```bash
# 전면 차단 (재배포 불필요, Lambda 콘솔에서)
SAJU_LOCALIZED_VOICE=0

# 언어별 조정
SAJU_LOCALIZED_VOICE_LANGUAGES=fr,ja
```

### 5.2 언어를 추가하는 절차

`harmful-phrases-i18n.js`의 `PENDING_MEASUREMENT` 주석에 적혀 있다.

1. 기준선 2회 측정. **50% 이상이면 거기서 멈춘다** (얻을 것이 없다).
2. 50% 미만이면 ON 2회 측정.
3. **OFF 최고 < ON 최저** 일 때만 켠다. 평균으로 판정하지 않는다.
4. fallback이 늘면 켜지 않는다.

`contracts.test.js`가 데이터 기반이라 언어를 추가해도 테스트를 손볼 필요가 없다.

---

## 6. 남은 일

### 6.1 여전히 병목 (사람이 해야 함)

**`progress_2026-08-11.md` §7.1 — 언어별 실제 결제 QA.** 어제 표면 결함 8건이 전부
사람이 클릭해서 나왔고 자동 검증은 하나도 못 잡았다. 오늘 6개 언어의 리포트 내용이
바뀌었으므로 **ja·es는 특히 눈으로 볼 값어치가 있다.** 이게 끝나야 $9.99 인상 판단.

로컬 테스트는 반드시 `frontend/scripts/sandbox-serve.sh`로만 띄울 것.

### 6.2 기술 과제

- **vi 재판정** — 원국을 5개로 늘리거나 계측기가 더 조용해지면. 저작본은 이미 있다.
- **zh·es** — 같은 조건. `PENDING_MEASUREMENT`에 등록돼 있다.
- 루브릭(`localization-rubric.js`)을 **런타임 게이트로 승격** — 미달 생성물을 출력
  단계로 보내지 않는 구조. 데이터는 이미 분리해 두었으나 생성 비용·지연이 늘어난다.
- 리포트 전체의 nativeness가 대부분 4/5 — fr만 3/5. 번역 문제가 아니라 문체 문제다.

### 6.3 운영

- 샌드박스 테스트 행 3건 정리 여부 (어제부터 이월)
- staging Supabase 삭제 상태 (어제부터 이월)
- 측정용 PayPal 주문 다수 생성됨 — `deploy-verify@somyung.cc` 태그, capture 안 했으므로
  과금 없음, 자동 만료

---

## 7. 이번 세션 커밋 (8/12)

```
2718f63 feat(reports): enable authored Thai lines; six of nine languages now localized
f8c6194 docs: hold Vietnamese, and show why the median was not enough
85bd917 docs: record the full per-language verdict table and the range-based rule
ee76c61 feat(reports): enable authored Indonesian lines, and write down the decision rule
6098d3e feat(reports): enable authored Portuguese lines, hold Chinese and Spanish
b9e4dd3 docs: the Korean leak had exactly two sources, both now closed
2dfcea1 chore: ignore measurement and build artifacts
35079b9 docs: record the presentation-fallback rate, including 50% on Japanese
05a0b0d feat(reports): localize the element remedy table for the remaining six languages
ef5a8cb docs: record the analytic-data localization result
ca7f22f fix(reports): localize the element remedy table and element names
5a808c7 feat(reports): author parent lines for all 9 languages, enable English
b3b42bd feat(reports): write Japanese parent lines natively, and stop leaking Korean
11fc210 fix: give the localized-voice path an off switch in production
efef790 feat(reports): write French parent lines natively, measured 30% -> 56%
1f9cfab test: rebuild the localization instrument, and add per-language rubrics
9208ddd test: measure the non-Korean grounding loss, and record two failed fixes
2535a32 fix: raise the free-preview limit, and make retry-path drift impossible
```

---

## 8. 다음 세션에서 주의할 것

어제 문서 §9의 항목들은 그대로 유효하다. 오늘 추가된 것:

1. **계측기를 먼저 검증하지 않으면 개입 판정이 전부 무의미하다.** 이번에 "개입 2건
   모두 실패"라고 결론 내렸다가, 계측기를 고친 뒤 다시 재서 뒤집었다. 같은 산출물이
   50%와 77%로 나오는 도구로는 아무것도 판정할 수 없다.
   → 새 측정을 만들면 **먼저 자기일관성부터 재라.**

2. **평균으로 판정하지 말 것.** 생성 잡음이 원국당 29pt까지 흔들린다. 범위가
   분리됐는지(OFF 최고 < ON 최저) 보는 편이 훨씬 안전하다. 베트남어가 그 증거다.

3. **비교하는 두 조건의 시행 횟수를 맞출 것.** 한쪽만 더 많이 돌리면 그쪽 최대값이
   우연히 높게 나온다.

4. **측정 라벨을 재사용하지 말 것.** `--label=v2`를 두 언어에 재사용해 한국어 결과를
   덮어썼고, 그 탓에 "한국어도 nativeness 3/5"라는 잘못된 관찰을 한동안 들고 있었다.

5. **참고 블록에 번호 목록·소제목·불릿을 쓰지 말 것.** 지시문을 번호 목록으로
   썼더니 모델이 그 구조를 출력 템플릿으로 모방해 라벨 계약을 깼다(6건 중 4건
   fallback). 내용은 그대로 두고 형태만 평문으로 낮추자 0/3이 됐다. **참고 블록은
   내용뿐 아니라 생김새도 출력에 영향을 준다.**

6. **이 환경의 git 출력을 믿지 말 것.** RTK 훅이 커밋 해시와 메시지를 뒤섞어
   보여준다. 실제로 존재하지 않는 해시와 가짜 푸시 성공 메시지를 보고 작업이
   덮어써졌는지 한참 조사했다(결과: 멀쩡했음).
   → **git 상태는 파일로 덤프해 Read로 읽거나, 원격을 새로 클론해 확인할 것.**

7. **Bash 도구는 cwd가 호출 간 유지되고, 타임아웃 최대 10분이다.** `cd backend` 후
   다음 호출에서 상대경로를 쓰면 `backend/frontend/...`로 잘못 해석된다. 10분을
   넘길 작업은 `run_in_background`로.
