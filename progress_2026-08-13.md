# SoMyung Progress — 2026-08-13

전날(`progress_2026-08-12b.md`)의 §10~12에서 이어진다. 이 문서는 8/13 하루치이고,
품질·포지셔닝 판단의 전문은 `SoMyung_품질_포지셔닝_진단_2026-08-13.md`에 있다.

---

## 1. 한 줄 요약

**측정이 없던 곳을 재고, 재서 나온 결함을 고치고, 그 위는 사람이 아니라 스케줄이
보게 만들었다.** 유료 리포트에 8지표 측정을 신설(전에는 프리뷰만 있었다), 거기서
나온 최대 발견은 **리포트 15건 전부가 아이 이름을 0회 불렀다**는 것. 문체를 예문으로
끌어올리고(warm 2.6→3.1, 10개 언어), 마지막으로 별점·환불 수집 체계를 붙여 주간
다이제스트를 자동 발송으로 돌렸다.

---

## 2. 오늘 커밋 (9건, 전부 배포됨)

```
6e2fada docs: the weekly routine is a schedule now, not a checklist
12a2d41 feat(signals): run the weekly routine on a schedule instead of by hand
7939a2f docs: record the user-signal system and what it found on first run
fafb4a2 feat(signals): collect what paying readers actually say — ratings and refunds
27e087d docs: record the voice-intervention measurements and their honest limits
47c8902 feat(reports): move the paid report's voice from form to letter — measured
1f2e4a2 docs: quality-and-positioning assessment — what hit 100%, what remains
ca0aa65 feat(reports): call the child by name — the paid report never did
49bb538 fix(reports): measure the paid report the way the preview was measured — and fix what it found
```

---

## 3. 확정된 사실

### 3.1 돈 받는 산출물에 측정이 없었다

프리뷰는 6지표로 100%를 만들어 두고(8/12 §11), **유료 리포트에는 같은 측정이
없었다.** zh 간체 수정도 유료 프롬프트에 들어갔지만 유료 출력으로 확인된 적이 없었다.
검증 순서가 거꾸로였다.

`backend/scripts/measure-premium-artifacts.js` 신설(5→10개 언어 × 3원국, 결정론 8지표).
찾은 것:

| 결함 | 규모 |
|---|---|
| **아이 이름 호명 0회** | **15/15 리포트 전원** |
| `丁酉（丁酉）` 한자 자기중복 | zh 1건 ×4회 |
| `1) 1.` 이중번호 | zh 1건 ×6회 |
| 겹괄호 `丁酉（（丁酉））` | ja 1건 ×6회 |

이름은 표지(presentation)에만 전달되고 프롬프트는 "이 아이"로만 말하고 있었다.
부모가 산 자기 아이의 리포트가 이름을 한 번도 안 부르면 범용 문서로 읽힌다.

### 3.2 이름 하나가 세 곳을 건드렸다

이름을 프롬프트에 넣자 옆이 세 번 움직였다. 전부 선제 차단:

1. **한글 이름이 잔여물 제거기에 걸린다** — "민서"는 2글자라 조각 규칙에 정확히
   걸린다 → keepWords 화이트리스트.
2. **오염율 게이트가 이름을 오염으로 센다** — 이름을 열 번 부른 정상 리포트가 한도에
   걸려 재생성을 돈다 → hangulRatio에서 이름 제외.
3. **ja/zh/th PDF에 한글 글리프가 없다** — NotoSansJP·SC·ThaiLooped 전부(fontkit
   확정). **이름 호명 전에도 표지가 이미 밟고 있던 함정이다.** 태국어용 문자종 분할
   래퍼를 일반화해 한글 런을 NotoSansKR로 보낸다. ja·zh·th PDF 실렌더로 확인.

### 3.3 지시문은 또 실패했고 예문은 또 성공했다 (세 번째 재현)

| 개입 | warmth (ko) | 판정 |
|---|---|---|
| 기준선 | 2.67 | — |
| voice charter (지시문) | 2.67 | **효과 0** — 8/11 §4.1과 같은 패턴 |
| + 도입부 문체 예문 | **3.33** | 2회 실행 일치 |

10개 언어 집계: nat 3.3→3.9 · warm 2.6→3.1 · **spec 3.4→4.1**(과교정 감시축이
오히려 올랐다 — 산문화가 내용을 비우지 않았다). fr 최대 수혜(nat 2.67→4.0),
ja 변화 없음, **en은 심판 노이즈(동일 텍스트 재채점 ±0.67)로 미확증**.

예문의 대가로 회귀 2건: `## 1.` 헤딩 생략(전체 fallback), s5 산문화(라벨 계약 붕괴).
"형식은 계약대로" 명시로 상쇄. **예문은 형태까지 베끼게 한다 — 계약 리마인더 필수.**

### 3.4 환불률 0%가 아니라 측정되지 않고 있었다

환불 처리 코드는 웹훅 경로에 있는데, `PAYPAL_WEBHOOK_ID`가 없으면 프로덕션 웹훅은
fail-closed로 **거부**된다(`middleware/webhookVerify.js`). 그 값은 .env에 없다.
즉 오늘 환불이 나도 DB는 `completed`로 남는다.

부수 버그: 환불 웹훅이 `payments.metadata`를 통째로 덮어썼다. 거기 구매자 이메일과
product_type이 들어 있고 리포트 소유 확인이 그것을 읽는다 — **환불한 사람이 자기
리포트를 못 여는 경로**였다. 병합으로 수정.

---

## 4. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포, `verify:live` 14/14 |
| 프론트엔드 | 최신 배포(별점 위젯 10개 언어 라이브) |
| 테스트 | **219건 / 16 suites** |
| 미커밋 | 0 (untracked 4건은 codex 산출물·전략문서, 의도적) |
| 브랜치 | `codex/somyung-launch-ready` (main 미머지) |
| migration | 008까지 실행 완료 |
| 주간 잡 | EventBridge `cron(0 0 ? * MON *)` **ENABLED**, 실호출 검증 완료 |

### 측정 자산 (재실행 가능, 전부 저장소에 있음)

| 스크립트 | 재는 것 | 비용 |
|---|---|---|
| `measure-preview-core.js` | 프리뷰 6지표 (10언어×5원국) | AI 생성 |
| `measure-premium-artifacts.js` | 유료 8지표 (10언어×3원국) | 캐시 재사용 |
| `measure-report-voice.js` | 문체 3축 (심판 3회 중앙값) | 캐시 위에서만 |
| `report-signals.js` | 별점·환불·전달 | **AI 0** |
| `sync-payment-status.js` | PayPal 실상태 | **AI 0** |

캐시: `output/premium-artifacts/`(44건), 기준선 `output/premium-artifacts-baseline/`,
문체 실행 기록 `output/voice/`(8건).

---

## 5. 최종 지표

**유료 리포트 8지표 — 10개 언어 × 3원국 전부 통과**
ready · 정체성 일치 · 한글 혼입 0 · 영어 잔류 0 · 한자중복 0 · 이중번호 0 ·
이름 3회 이상 · zh 간체 정합.

**프리뷰 6지표 — 두 번 연속 실행 동일**
표제 일치 50/50 · 편중 언급 50/50 · 한글 0/45 · 영어 0/50 · 한자중복 0/50 ·
제목형식 0/50.

---

## 6. 다음 세션에서 할 수 있는 것

### 6.1 기다리는 것 (지금은 아무것도 안 해도 된다)

**신호가 쌓이기를 기다린다.** 주간 다이제스트가 월요일마다 메일로 온다.
판정 기준은 다이제스트가 스스로 경고한다 — **결제 30건 전까지 환불률은 방향
지표일 뿐이고, 프롬프트 버전 비교는 버전당 평가 20건부터.**
현재 표본: 결제 완료 4건(90일), 평가 0건, 프롬프트 v2 리포트 0건.

### 6.2 신호가 말해주면 할 것

- 별점이 낮은 언어·프롬프트 버전이 드러나면 그 지점만 다시 잰다.
- 저평가 코멘트는 다이제스트에서 **낮은 별점부터** 정렬돼 온다.

### 6.3 신호와 무관하게 남은 것

- **warmth 3.1 천장** — 라벨 계약(파서가 요구하는 30+ 필드)을 유지하는 한 "완전한
  편지"는 구조적으로 불가능. 더 올리려면 presentation 계약 자체에 산문 슬롯을
  넓혀야 하고, 파서·웹·PDF가 전부 걸린다. **별도 결정 필요.**
- **가격 실험** — `deluxe` 티어 코드는 있으나 안 팔린다. 사주랩이 ₩1M을 받는
  시장에서 ₩4,900은 자격(1급+석사) 대비 저가. EN만 $7.99로 올리는 실험이 가장 싸다.
  (진단 문서 §3.2)
- **카피에 차별점이 없다** — The Pattern의 최대 비판이 "블랙박스"인데 우리는 계산
  근거를 이미 보여준다. 마케팅 어디에도 안 쓰고 있다.
- `/saju/reading-check`가 `select('*')`로 행 전체 반환(유출은 아니나 컬럼 명시 권장)
- 태국어 legacy PDF 표지 헤더 겹침(fallback 경로, th 사용자 0명)
- `fortuneCycles` 한국어 121필드(렌더링 안 됨)
- 백엔드 `npm run lint` 깨져 있음 — eslint 설정 파일 없음
- 전략문서 `SoMyung_컴플라이언스_그로스_전략.md` §3.1은 낡음(영문 about은 이미 영어).
  **Week 1 항목은 실행 전에 라이브에서 하나씩 확인할 것.**

### 6.4 하지 않기로 유지

- PayPal 웹훅 등록 — 폴링이 매주 진실을 가져온다. 이 결제량에서 주 단위 지연은 무의미
- 루브릭 런타임 게이트 — 리포트마다 영구 비용, 막을 불량 미관측
- vi 재판정 — 대상 사용자 0명

---

## 7. 절차로 남길 교훈 (오늘 세 번 반복된 것)

1. **돈 받는 산출물부터 잰다.** 프리뷰를 100%로 만든 지표가 유료에는 없었다.
2. **지표는 산출물 형식에 맞춰 다시 설계한다.** 프리뷰의 "첫 오행 언급" 휴리스틱을
   유료에 그대로 쓰면 정상 리포트 3건을 오판한다(부 기질은 설계다). 다이제스트도
   같은 실수를 했다 — presentation 도입 이전 리포트를 ready 분모에 넣어 18.2%로
   보였다(실제 40%).
3. **모든 개입에 과교정 감시 지표를 붙인다.** "편중을 쓰지 마라" → 편중 실종
   (98→26%). "이름을 불러라" → 제거기·게이트·글리프 3중 충돌. 고친 자리 옆이 움직인다.
4. **프롬프트에 보여준 형태는 그대로 베껴진다.** 금지 문구를 더하는 것보다 베낄
   형태를 지우는 것이 빨랐다(영어 잔류 25% → 지시 추가로 25% → 형태 삭제로 0%).
5. **"주 1회 하세요"는 운영이 아니다.** 스케줄로 옮길 수 있으면 옮긴다.

---

## 8. 마케팅 착수 판단 (세션 말미 논의)

### 8.1 컴플라이언스는 전략문서보다 상태가 좋다

라이브 `https://somyung.cc/en/privacy/` 실측 — 9개 섹션 전부 존재:
GDPR Art. 6 법적근거 · 아동데이터/보호자 · 국외이전 · 보유기간 · 보안 · 정보주체 권리 ·
CCPA/CPRA · Privacy Officer.

**즉 `SoMyung_컴플라이언스_그로스_전략.md` §3.3("영문 privacy policy 보강 2-3시간")도
§3.1과 마찬가지로 낡았다.** Week 1 5항목 중 최소 2개가 이미 되어 있다.
**EN 트래픽을 태우는 데 법적 장애물은 없다.**

> 반복 확인: 그 전략문서의 진단은 실행 전에 항목별로 라이브에서 확인할 것.
> 두 번 다 "해야 한다"고 적힌 것이 이미 되어 있었다.

### 8.2 지금이 적기인 이유

- 품질은 결정론 8지표 100%, v2 문체 프롬프트 라이브
- **신호 체계는 트래픽 없이는 영원히 안 쌓인다.** 판정 기준이 결제 30건인데 90일 4건.
  마케팅이 곧 신호 수집기다

### 8.3 다만 — 캠페인 전에 퍼널 계측 (다음 세션 1순위)

지금 다이제스트가 보는 것: 리포트 수 · 결제 · 별점.
**안 보이는 것: 몇 명이 프리뷰까지 왔는가.**

- 프리뷰는 설계상 DB에 저장하지 않는다(개인정보 약속) → 카운터도 없다
- GA는 있으나 동의 게이트 뒤 → 거부한 사용자는 안 잡힌다

그래서 **캠페인을 돌려도 효과 판정이 불가능하다.** 유입 100명이 프리뷰에서 이탈했는지
가격에서 이탈했는지 구분할 수 없다. 오늘 하루 반복한 교훈 그대로 —
**측정 없이 개입하면 배우는 것이 없다.**

제안: **PII 없는 퍼널 카운터**(반나절). 프리뷰 요청 / 결제 시작 / 결제 완료를
언어별·일별 집계만. 개인정보 저장 0, 기존 주간 다이제스트에 붙인다.

### 8.4 캠페인 자체에 대한 판단

- **유료 광고는 아직.** 전환율 데이터 0 → CAC 계산 불가 → 돈만 태운다.
  퍼널 데이터 2~3주 쌓인 뒤 판단.
- **제로예산부터.** 이미 있는 자산: 블로그 18편 · 콘텐츠 엔진
  (`scripts/content-repurpose/`) · GEO 75 · MCP 서버 npm 공개.
- **시장은 ko/ja/en만.** 나머지 7개 언어는 실사용자 0명 — 마케팅 대상이 아니다.

권장 순서: **퍼널 계측 → 콘텐츠 엔진 실행 → (데이터 보고) 유료 채널 판단**

에이전트가 할 수 있는 것: 퍼널 계측, 콘텐츠 엔진 실행(블로그·SNS 초안),
GEO/SEO 갭 분석, 랜딩 카피 개선.
사람이 해야 하는 것: SNS 계정 개설, 인플루언서 outreach, Product Hunt, 광고 계정.
