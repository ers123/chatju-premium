# SoMyung / ChatJu Premium Progress — 2026-08-04

> **2026-08-10 추가 기록.** 아래 본문은 8/4 시점 기준이며, 그 뒤 리포트 품질·다국어 작업이
> 진행되어 일부 내용이 낡았습니다. 변경분은 문서 맨 끝 「13. 2026-08-10 업데이트」를 보세요.
> 특히 §7의 staging Supabase 항목은 더 이상 유효하지 않습니다(프로젝트 삭제됨).

작성 기준: 2026-08-04 KST  
대상 checkout: `/Users/yohan/Projects/fortune/chatju-premium`  
현재 branch: `codex/somyung-launch-ready`  
작성 목적: 지금까지의 SoMyung 서비스 진행 상황을 결제 과정, 프리미엄 리포트 생성, PDF/웹 산출물, 배포 상태까지 한 문서로 정리한다.

---

## 1. 현재 결론

SoMyung은 현재 실서비스 도메인 `somyung.cc`에서 Cloudflare Pages 프론트엔드와 AWS Lambda/API Gateway 백엔드 조합으로 운영 중이다. 무료 미리보기, 결제/프로모션 진입, 프리미엄 리포트 생성, 결과 조회, PDF 다운로드까지의 주요 제품 흐름은 코드상 연결되어 있으며, 2026-07-22~2026-07-23 사이에 리포트 품질 개선과 staging E2E 산출물 검증, production 배포가 진행되었다.

다만 “상용 과금 서비스로서 완전히 끝났다”라고 보기에는 아직 두 가지가 남아 있다.

1. 실제 유료 결제 샘플을 통한 운영 QA
   - 현재 확인된 staging 산출물은 주로 promo/entitlement 기반 프리미엄 생성 검증이다.
   - PayPal create/capture 구현은 되어 있으나, 실제 카드/PayPal 승인까지 포함한 운영 샘플 결제 검증은 별도 운영 QA로 남겨야 한다.

2. 실제 provider 산출물의 사람 눈 QA
   - structured renderer, 다국어 label contract, PDF/web rendering path는 배포되어 있다.
   - 그러나 부모가 실제 구매 후 받는 결과물이 “고급스럽다/전문적이다”라고 느껴지는지는 한국어 1건 + 비한국어 1~2건의 실제 산출물 QA가 필요하다.

---

## 2. 현재 운영 배포 상태

### Frontend

- Hosting: Cloudflare Pages
- Production domain: `https://somyung.cc`
- 현재 읽기 확인:
  - `https://somyung.cc/` → HTTP 301, `/en/`으로 redirect
  - `https://somyung.cc/saju/input/` → HTTP 200
  - `https://somyung.cc/payment/` → HTTP 200
- 최신 확인된 Cloudflare Pages production deployment:
  - Deployment ID: `a3b61ea9-0aa6-4f91-8041-dddc237443d4`
  - Branch: `somyung`
  - Source commit: `c5c508e`
  - Deployment URL: `https://a3b61ea9.somyung.pages.dev`

### Backend

- Runtime: Express + Node.js on AWS Lambda/API Gateway
- Production API root:
  - `https://0eo64hyuv7.execute-api.ap-northeast-2.amazonaws.com/`
- 현재 읽기 확인:
  - HTTP 200
  - `environment: production`
  - `version: 1.1.0`
  - exposed endpoint summary includes:
    - `/fortuneTell`
    - `/auth/*`
    - `/saju/calculate`
    - `/saju/calculate-promo`
    - `/payment/*`
    - `/promo/validate`
    - `/admin/*`

---

## 3. 사용자 제품 흐름

현재 제품 흐름은 다음 구조다.

```text
Landing / localized site
  ↓
Saju input
  ↓
Free preview
  ↓
Payment page 또는 promo code
  ↓
PayPal create/capture 또는 promo entitlement
  ↓
Premium calculation
  ↓
Results page
  ↓
PDF download
```

### 무료 미리보기

- Endpoint: `POST /saju/preview`
- 인증 불필요
- 자녀 생년월일/시간/성별, 선택적으로 부모 정보와 언어를 받아 무료 preview를 생성한다.
- 관계 중심 preview 문구가 다국어로 구성되어 있다.
- 목적은 “무료 맛보기 → 프리미엄 전환”이다.

### 프리미엄 생성

- Endpoint: `POST /saju/calculate`
- 조건:
  - 완료된 결제 row가 있어야 한다.
  - 로그인 사용자이거나, 서버가 발급한 `paymentAccessToken`이 있어야 한다.
- 내부 검증:
  - payment row 조회
  - `status === completed` 확인
  - product type / amount / currency 검증
  - 사주 계산
  - AI interpretation 생성
  - structured presentation adapter 통과 시 premium renderer용 구조 저장

### Promo 기반 프리미엄 생성

- Endpoint:
  - `POST /promo/validate`
  - `POST /saju/calculate-promo`
- 인증 불필요
- 이메일과 promo code로 프리미엄 리포트를 생성할 수 있다.
- client-side `claimKey`를 서버가 sha256으로 저장하고, 결과 page에서 `reading-check?claim=...`로 polling할 수 있다.
- staging 산출물은 이 promo/entitlement 경로 검증 증거가 가장 강하다.

### 결과 조회 / PDF

- 결과 page는 sessionStorage의 completed payment/promo 정보와 claim key를 사용해 premium result를 조회한다.
- PDF download route:
  - `GET /saju/reading/:id/pdf?token=...`
- frontend download wrapper:
  - `frontend/components/report/ReportPdfDownload.tsx`
- PDF copy는 지원 언어별로 localized 되어 있다.

---

## 4. 결제 과정 현재 상태

### 현재 payment provider

현재 코드상 실제 결제 provider는 PayPal 중심이다.

- Backend route:
  - `POST /payment/paypal/create`
  - `POST /payment/paypal/capture`
  - `POST /payment/paypal/webhook`
- Frontend payment page:
  - PayPal JS SDK
  - Google Pay through PayPal SDK
  - promo code flow

### 결제 create

`POST /payment/paypal/create`는 다음을 수행한다.

1. email 필수 확인
2. client가 보낸 amount는 참고값으로만 취급
3. `product_type`으로 server-side product catalog 조회
4. PayPal order 생성
5. Supabase `payments` row 생성
6. server-issued `paymentAccessToken` 발급
7. client에 `paypalOrderId`, `approvalUrl`, `paymentAccessToken` 반환

중요한 점: 가격은 client가 정하지 않는다. `backend/src/config/products.js`의 catalog가 권위 source다.

### 결제 capture

`POST /payment/paypal/capture`는 다음을 수행한다.

1. `paypalOrderId` 확인
2. `paymentAccessToken` 필수 확인
3. token의 purpose/paypalOrderId/product/amount/currency 검증
4. 이미 completed인 payment면 idempotent하게 기존 payment 반환
5. PayPal capture API 호출
6. PayPal capture amount/currency가 product catalog와 일치하는지 검증
7. Supabase payment row를 `completed`로 업데이트
8. client에는 최소화된 payment object 반환

### 다국어 가격 catalog

현재 frontend/backend 가격 catalog는 다음 방향으로 구성되어 있다.

| Locale | Checkout 상태 | Currency | Amount | Product type |
|---|---:|---:|---:|---|
| ko | PayPal checkout 비활성 | - | - | free/promo 중심 |
| en | 활성 | USD | 4.99 | `premium_saju` |
| ja | 활성 | JPY | 490 | `premium_saju_jpy` |
| zh | 활성 | USD | 4.99 | `premium_saju` |
| vi | 활성 | USD | 4.99 | `premium_saju` |
| id | 활성 | USD | 4.99 | `premium_saju` |
| es | 활성 | EUR | 3.49 | `premium_saju_eur_349` |
| pt | 활성 | EUR | 3.49 | `premium_saju_eur_349` |
| fr | 활성 | EUR | 3.99 | `premium_saju_eur_399` |
| th | 활성 | THB | 89 | `premium_saju_thb` |

한국어 locale은 현재 PayPal 유료 checkout이 꺼져 있다. 이유는 PayPal 수취 가능 통화와 KRW 운영 이슈 때문이며, 현재 한국어는 무료/promo 중심으로 열려 있다. 한국어 유료 과금을 실서비스로 열려면 국내 PG 또는 지원 통화 기반 가격 정책을 별도로 확정해야 한다.

### 결제 관련 known issue

현재 code inspection 기준으로 보조 조회 helper에 route naming mismatch가 있다.

- frontend `getPaymentByOrderId()`는 `/payment/order/${orderId}`를 호출한다.
- backend route는 `GET /payment/:orderId` 형태다.
- frontend `getUserPayments()`는 `/payment/history`를 호출한다.
- backend route는 `GET /payment/history/me` 형태다.

핵심 checkout flow인 PayPal button → create → capture → results 이동에는 직접적인 영향이 없을 가능성이 높지만, 결제 상태 조회/히스토리 UI를 사용할 경우 정리해야 한다.

---

## 5. 프리미엄 리포트 품질 개선 진행 상황

### 문제 인식

초기 PDF/result는 다음 문제가 있었다.

- dense prose가 많고 카드/섹션 위계가 약했다.
- markdown syntax가 일부 노출될 수 있었다.
- 부모 입장에서 “바로 써먹는 양육 카드”보다는 일반 운세형 글처럼 보일 위험이 있었다.
- page flow와 spacing이 고급스럽지 않았다.
- reference 서비스와 비교하면 첫인상/전문성이 약했다.

개선 방향은 reference copy가 아니라 SoMyung 고유의 parenting-oriented field guide로 잡았다.

핵심 원칙:

```text
visible basis → observable behavior → parent action
```

즉, 사주 원국을 부모가 바로 관찰 가능한 행동과 대화법으로 바꾸는 리포트다.

### 적용된 주요 commit

- `7c39170 feat(reports): add deterministic premium presentation flow`
  - structured presentation path 도입
  - 기존 free-form markdown을 premium report 카드 구조로 변환
  - fallback/unsafe/provider drift 방어 시작

- `3d0c77e fix(reports): accept real provider premium markdown variants`
  - 실제 provider가 생성하는 markdown 변형 대응

- `bf91af2 fix(reports): accept provider staging markdown variants`
  - staging provider markdown 변형 대응

- `b169e23 fix(reports): accept browser provider report variants`
  - browser route 산출물 변형 대응

- `47cedd4 feat(reports): redesign premium editorial experience`
  - premium editorial report visual/system 개선

- `c5c508e feat(reports): add multilingual premium presentation`
  - 다국어 structured presentation contract 적용
  - web/PDF renderer가 `presentation.locale`, `presentation.ui`를 소비하도록 변경

- `7da34ba ci: keep staging Supabase active`
  - staging Supabase free-plan pause 방지를 위한 keep-alive workflow 추가

### 현재 structured report 상태

현재 structured report는 provider output이 계약을 만족할 경우 `presentationStatus === ready`로 web/PDF 전용 구조를 렌더링한다.

지원 언어:

- Korean: `ko`
- English: `en`
- Japanese: `ja`
- Chinese: `zh`
- Vietnamese: `vi`
- Indonesian: `id`
- Spanish: `es`
- Portuguese: `pt`
- French: `fr`
- Thai: `th`

공통 contract:

- 9개 ordered sections
- localized required labels
- calculated basis
- observation/action fields
- parenting-card/script fields
- unsafe claim scanning
- duplicate-content checks
- fallback path 유지

Renderer:

- Web: `frontend/components/saju/PremiumEditorialReport.tsx`
- PDF: `backend/src/services/pdf.service.js`
- Adapter/contract: `backend/src/services/report-presentation.js`
- Prompt integration: `backend/src/services/saju.service.js`

---

## 6. 검증된 산출물

### Staging E2E 산출물

현재 workspace에 다음 산출물이 있다.

- `output/staging-e2e/full-flow-report.pdf`
- `output/staging-e2e/full-flow-report.txt`
- `output/staging-e2e/full-flow-contact-sheet.png`
- `output/staging-e2e/proof-ready-report.pdf`
- `output/staging-e2e/proof-ready-report.txt`
- `output/staging-e2e/proof-ready-contact-sheet.png`
- `output/staging-e2e/*-calculate-response.json`
- `output/staging-e2e/*-reading-check-response.json`
- `output/staging-e2e/*-db-verification.json`

대표 evidence:

| Artifact | 상태 |
|---|---|
| `full-flow-summary.json` | readingId 생성, PDF 생성, 약 25초 |
| `ready-flow-summary.json` | readingId 생성, PDF 생성, 약 26초 |
| `proof-ready-summary.json` | readingId 생성, PDF 생성, 약 25초 |
| `proof-ready-db-verification.json` | `readingLookup: ok`, `presentationStatus: ready`, `presentationSections: 9` |

주의:

- `full-flow-db-verification.json`에는 `presentationStatus: fallback`, `presentationStatusReason: unsafe_claim` 산출물도 남아 있다.
- 이는 안전 필터/adapter fallback이 실제로 작동했다는 증거이기도 하지만, “모든 provider 산출물이 ready로 간다”는 의미는 아니다.
- `proof-ready-*` 계열은 ready path 검증 증거로 보는 것이 맞다.

### Local test/build 검증

가장 최근 리포트 다국어 rollout 당시 확인된 검증:

- focused presentation test: 43 tests PASS
- backend full test: 9 suites / 114 tests PASS
- backend build PASS
- frontend TypeScript PASS
- frontend build PASS
- `git diff --check` PASS

### Live surface 검증

2026-08-04 현재 읽기 전용 확인:

- `https://somyung.cc/saju/input/` HTTP 200
- `https://somyung.cc/payment/` HTTP 200
- backend API root HTTP 200, production env response
- Cloudflare latest production deployment source: `c5c508e`

---

## 7. Supabase / staging 상태

Staging Supabase project가 별도로 준비되었다.

- Staging URL: `https://efjdydglfvwkodztiove.supabase.co`
- `backend/.env.staging`이 workspace에 존재한다.
- Supabase dashboard의 새 key 체계에서는:
  - Publishable key → frontend/browser-safe anon role에 해당
  - Secret key → backend-only privileged key에 해당
- secret 값은 문서에 남기지 않는다.

추가로 `7da34ba ci: keep staging Supabase active`가 들어가 있다.

목적:

- Supabase free-plan staging project가 비활성화되지 않도록 주기적 read request를 보내는 workflow 준비
- GitHub Secrets 기반:
  - `STAGING_SUPABASE_URL`
  - `STAGING_SUPABASE_ANON_KEY`
- 읽기는 최대 1 row 수준이며, body는 저장하지 않는 방향이다.

단, GitHub Actions에서 실제로 계속 실행 중인지는 현재 문서 작성 시점에서 live verification을 별도로 하지 않았다.

---

## 8. 보안/운영 guardrail

현재까지 적용/확인된 주요 guardrail:

- AWS HTTP API throttling
- Lambda reserved concurrency cap
- Cloudflare WAF rule for impossible WordPress/PHP/CGI reconnaissance
- payment create/capture rate limit
- webhook rate limit
- PayPal webhook verification middleware path
- server-issued `paymentAccessToken`
- completed payment status check before premium calculation
- product/amount/currency mismatch rejection
- claim key raw value 미저장, sha256 hash 저장
- report lookup/PDF tokenized access path
- 개인정보 삭제 시 법정 보존이 필요한 payment record는 최소화/보존하는 정책 반영

주의:

- Cloudflare는 `somyung.cc` site traffic을 보호한다.
- AWS Lambda Function URL/API Gateway 경로는 Cloudflare와 별도 perimeter이므로 backend-side rate/concurrency guardrail이 중요하다.

---

## 9. 현재 남은 작업

### A. 실제 결제 QA

우선순위가 가장 높다.

해야 할 검증:

1. non-Korean locale에서 PayPal button 노출 확인
2. sandbox 또는 소액 production 결제 create 확인
3. PayPal approval 후 capture 확인
4. `payments.status = completed` 확인
5. `/saju/calculate`가 completed payment로 premium reading 생성하는지 확인
6. 결과 page 이동 확인
7. PDF 다운로드 확인

한국어 유료화를 하려면 먼저 결제 정책 결정이 필요하다.

- 현재 `ko`는 PayPal checkout 비활성이다.
- 국내 결제라면 PortOne/KakaoPay/NHN KCP 등 PG 경로를 별도 결정해야 한다.
- 또는 한국어도 USD 등 PayPal 지원 통화로 받을지 정책 결정이 필요하다.

### B. 실제 산출물 QA

추천 샘플:

1. Korean 1건
2. English 1건
3. Japanese 또는 Chinese 1건

체크 항목:

- markdown syntax 노출 여부: `**`, `#`, raw bullet, raw checkbox
- card/box overflow
- PDF page break 품질
- parent-facing actionability
- “사주풀이”가 아니라 “부모용 실전 가이드”로 읽히는지
- calculated basis가 과도하게 미신/단정처럼 보이지 않는지
- fallback 발생 여부
- language-specific label이 섞이지 않는지

### C. Known issue 정리

작은 follow-up patch 후보:

1. `frontend/lib/api.ts`의 payment status/history helper route 정리
   - `/payment/order/${orderId}` vs backend `/:orderId`
   - `/payment/history` vs backend `/history/me`

2. 한국어 paid checkout 정책 정리
   - 현재 `ko: null`
   - 유료 출시 대상이 한국 부모라면 가장 큰 비즈니스 blocker다.

3. Email delivery 상태 확인
   - staging DB verification 일부에서 `emailStatus: failed`가 확인된다.
   - PDF 결과 page가 직접 제공되더라도, 구매 후 이메일 전달을 상품 약속에 넣을 경우 deliverability는 별도 QA가 필요하다.

---

## 10. 다음 추천 순서

실행 순서는 다음이 합리적이다.

1. payment route helper mismatch의 작은 patch
2. PayPal sandbox 또는 소액 production 결제 1회 end-to-end
3. Korean 유료화 정책 결정
4. 실제 Korean premium report 1건 QA
5. English/Japanese 또는 Chinese premium report 1~2건 QA
6. 문체/spacing/fallback 결과에 따라 마지막 polish patch

이 순서가 맞는 이유:

- 지금 구조 자체를 크게 다시 짤 필요는 없다.
- 실제 병목은 “리포트 구조”보다 “결제 완료 후 실제 산출물의 상용 품질”이다.
- 한국어 paid checkout 정책은 출시 가능성에 직접 영향을 준다.

---

## 11. 상태 요약

| 영역 | 현재 상태 | 판단 |
|---|---|---|
| Frontend live | `somyung.cc` HTTP 200 | 운영 중 |
| Backend live | production root HTTP 200 | 운영 중 |
| Free preview | 구현됨 | 사용 가능 |
| Promo premium | 구현 및 staging evidence 있음 | 사용 가능 |
| PayPal create/capture | 구현됨 | 실제 결제 QA 필요 |
| Korean paid checkout | 비활성 | 정책/PG 결정 필요 |
| Premium report structured renderer | 구현/배포됨 | 실제 샘플 QA 필요 |
| Multilingual report contract | 10개 언어 구현/테스트됨 | provider real sample QA 필요 |
| PDF download | 구현 및 staging PDF evidence 있음 | 실구매 flow QA 필요 |
| Email delivery | 일부 staging failed evidence 있음 | 별도 확인 필요 |
| Staging Supabase | 별도 project/env 존재 | keep-alive workflow 준비됨 |

---

## 12. 한 줄 결론

SoMyung은 “무료 preview → 결제/promo entitlement → 프리미엄 리포트 → PDF” 제품 골격과 리포트 품질 개선 구조는 배포된 상태다. 다음으로는 코딩을 크게 늘리기보다, 실제 결제 1회와 실제 리포트 2~3건을 통해 상용 QA를 닫고, 한국어 유료 결제 정책을 확정하는 것이 필요하다.

---

## 13. 2026-08-10 업데이트 — 리포트 품질 및 다국어

작성 기준: 2026-08-10 KST / 브랜치 `codex/somyung-launch-ready`

### 13.1 배포된 커밋

| 커밋 | 내용 |
|---|---|
| `69add3d` | 명리 지식 증류 5개 모듈 + 계산값 기반 선택기 |
| `e681c07` | 지식 주입을 언어 허용목록(ko)으로 제한 |
| `d7a7f56` | 일본어 한글 혼입을 증폭시킨 라벨 지시문 되돌림 |
| `570abc0` | 섹션 지시문을 출력 언어로 작성 (다국어 근본 수정) |
| `bb49db8` | 파서 관용화 + 서식 실패 시 1회 재생성 |

### 13.2 §5 「프리미엄 리포트 품질」 후속 — 명리 근거 주입

8/4 문서는 리포트 구조 개선까지 다뤘지만, 실제 생성물을 측정해보니 **해석의 근거가 비어
있었다.** 한국어 샘플 9,095자에서 사주 고유 용어는 대운·세운 각 1회뿐이었고, 계산은
하면서도 프롬프트가 쓰지 않던 신호가 많았다 — 십성 분포(예시 아동: 관성 3=편관 과다,
재성 0), 사유 반합 금국, 십이운성(월지 제왕·년지 건록인데 일지는 사), 도화·역마, 화금 긴장.

`사주_reference` 코퍼스(통합교안 + 8장 발췌 + 안종선/김동완/양창순/김원/윤득헌)를 5개
모듈로 증류하고, 계산된 원국에 해당하는 조각만 결정론적으로 골라 주입하도록 했다.

- `backend/src/data/saju-knowledge/` — ten-gods, branch-relations, seasonal-tuning,
  element-patterns, parent-child
- `backend/src/services/saju-knowledge.js` — 선택기
- 롤백: `SAJU_KNOWLEDGE_DISABLED=1` / 언어 확대: `SAJU_KNOWLEDGE_LANGUAGES`

현재 **한국어에만 적용**되어 있다. 지식이 한국어로 주입되는데 일본어에서 그대로 새어
나왔기 때문이다. 언어를 넓히려면 해당 언어로 번역하거나 누수 여부를 언어별로 확인해야 한다.

### 13.3 §11 상태표 정정 — 「다국어 리포트 10개 언어 구현/테스트됨」은 사실이 아니었다

8/4 문서는 다국어 contract가 구현·테스트되었다고 적었으나, 실제 생성물을 측정하니
**비한국어 리포트는 전부 fallback이었다.** 유료 고객이 전량 비한국어라 이것이 가장 큰
품질 문제였다.

원인은 섹션 서식 지시문(`call1/2SectionInstructions`)이 한국어 전용인데 모든 언어의
프롬프트에 붙고, 그 안의 한국어 라벨 예시를 "리포트 언어로 번역해 쓰라"고 지시한 것이었다.
일본어는 번역 대신 한국어를 옮겨 적었고(한글 비율 최대 29.5%), 영어는 매번 새로 번역해
계약과 어긋나는 의역을 만들었다.

`premiumLabels`가 이미 출력 언어의 정확한 라벨을 갖고 있으므로, 영어 지시문을 만들고 그
문자열을 직접 끼워 넣어 번역할 여지를 없앴다.

### 13.4 구조적 취약성 — presentationStatus는 확률적이다

같은 코드·같은 사주로 반복 생성하면 결과가 갈린다(한국어가 5회 연속 ready 뒤 1회 fallback).
프롬프트 규칙을 추가하는 방식은 한 언어를 고치고 다른 언어를 깨뜨리는 진동만 낳아 중단했다.
대신 두 지점을 결정론적으로 만들었다.

- **파서 관용화**: 라벨을 줄 시작에서만 인식하던 것을 줄 어디서든 인식하도록 정규화
  (모델이 그룹 하나를 한 문단에 몰아 쓰는 경우를 살림). §5 강점 카드 개수를 세는 정규식이
  구분선 `---`을 4번째 카드로 오인하던 버그도 수정.
- **재생성**: 서식 문제일 때만 temperature 0.4로 1회 재생성, ready일 때만 채택.
  `unsafe_claim`은 재시도하지 않는다. 끄기: `SAJU_PRESENTATION_RETRY_DISABLED=1`

검증: 43개 샘플 스냅샷 대조 결과 회귀 0 / 개선 3 / 기존 ready 렌더 변경 0.
백엔드 테스트 140건, 리포트 파서 테스트 43건 통과.

### 13.5 §7 정정 — staging Supabase는 삭제되었다

`efjdydglfvwkodztiove.supabase.co`가 NXDOMAIN이다. 일시정지가 아니라 프로젝트가 존재하지
않는다. `.github/workflows/keep-staging-supabase-active.yml`이 없는 대상을 지키고 있다.
staging이 필요하면 새로 만들고 URL을 교체해야 한다. 프로덕션 Supabase는 정상.

### 13.6 §9 남은 작업 갱신

여전히 유효한 항목: 실제 PayPal 결제 1회 E2E, 결제 route helper 불일치
(`/payment/order/:id` vs `/:orderId`, `/payment/history` vs `/history/me`),
이메일 발송 실패, 한국어 유료화 정책.

새로 추가된 항목:
- **1차 통과율이 70%다.** 10개 언어 각 1회 생성 측정: 최종 10/10 ready이지만 그중 3건
  (en, zh, es)은 1차에 `partial_required_labels`로 실패한 뒤 재생성으로 구제됐다.
  재생성은 해당 리포트의 토큰을 2배 쓰므로 평균 비용이 약 30% 오른다. 1차 통과율 자체를
  올리는 후속 작업이 필요하다(파서 추가 관용화, 또는 실패한 섹션만 부분 재생성).
- 지식 주입을 비한국어로 확대하려면 모듈 번역 또는 언어별 누수 검증이 선행되어야 한다.
