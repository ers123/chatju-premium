# SoMyung Progress — 2026-08-13 (2부)

`progress_2026-08-13.md`의 §8.3에서 이어진다. 그 문서가 "다음 세션 1순위"로
지목한 퍼널 계측부터 시작해, 가격·콘텐츠·컴플라이언스까지 갔다.

---

## 1. 한 줄 요약

**측정을 켰고, 켜자마자 전제 세 개가 틀렸다는 게 드러났다.** 퍼널 계측을 붙이자
결제 데이터가 사실상 존재하지 않는다는 사실이 보였고(90일 5건 중 4건이 자체
테스트), 그래서 가격 저항 데이터가 없다는 근거로 가격을 4배 올렸다. 마지막으로
미성년자 데이터 처리를 10개 시장에 대해 조사하고 백엔드 실태를 대조해, 문서에만
있고 코드에 없던 약속 2건을 고쳤다.

---

## 2. 오늘 커밋 (2부, 5건 — 전부 배포됨)

```
b863d7b fix(privacy): 국외이전 동의 별도 기록 + 보존기간 삭제 자동화
759f30c docs: 10개 시장 미성년자 데이터 조사
dd1ff61 feat(signals): 결제 수단 없는 로케일의 구매 의향 카운트
0583d50 feat(pricing): 가격 인상 + 세 곳에 흩어진 가격 정합
a6192b4 feat(content): 콘텐츠 엔진 산출물을 게시 가능하게 + ko/ja 신설
1411ca4 feat(signals): 퍼널 계측  ← 1부 마지막
```

---

## 3. 확정된 사실

### 3.1 퍼널 계측 — 그리고 Lambda가 fire-and-forget을 삼킨다

`funnel_daily(day, event, language, hits)` + 원자적 증가 RPC. 저장하는 것은 네 칸이
전부다 — IP·UA·세션 식별자·생년월일 없음. 이벤트: `preview` / `checkout_start` /
`purchase` / `promo_report` / `purchase_intent`.

**첫 배포는 한 건도 세지 못했다.** 프리뷰가 200을 반환했는데 카운터는 비어 있고
에러 로그조차 없었다. 원인: fire-and-forget 프로미스가 serverless-http의 응답 종료
시점에 컨테이너와 함께 얼어 죽는다. **`report-job.js` 최상단이 같은 함정을 이미
적어 두었는데 그대로 밟았다.** 전 호출부를 await로 바꾸고, `trackFunnelEvent`
헬퍼를 삭제해 다시 못 만들게 했다(테스트로 고정).

> 교훈: **"이 저장소가 이미 아는 함정"을 먼저 읽는다.** 주석은 과거의 나에게
> 보내는 경고인데, 오늘의 내가 안 읽으면 아무 소용이 없다.

### 3.2 결제 데이터는 존재한 적이 없다

퍼널을 붙이고 나서야 기존 결제 데이터를 제대로 봤다:

| 사실 | 값 |
|---|---|
| payments 행 총계 | 157건 |
| 그중 8/11~8/13(우리 개발 세션) 생성 | **128건** |
| 완료(completed) | 5건 |
| 그중 동일 구매자가 8/11 하루에 만든 것 | **4건** |
| 실제 고객 결제로 보이는 것 | **1건** (2026-03-04) |

pending 152건이 jpy 43 / usd 34 / thb 22 / eur 22 / eur 21로 **5개 통화에 고르게**
퍼져 있다. 실사용자 분포(ko 12·ja 6·en 6)로는 나올 수 없는 모양 — 전부 QA 흔적이다.

**정정:** 세션 중반에 "148건 시도 중 4건 완료 → 결제 단계가 새고 있을 수 있다"고
판단했는데 **틀렸다.** 결제 누수의 증거는 없다.

### 3.3 한국은 결제 자체가 불가능했다 (메모리도 틀렸다)

`pricing.ts`의 `ko: null` — PayPal이 KRW를 수취하지 못해 **한국은 무료+프로모뿐**이다.
화면 문구는 `'₩4,900 (한국 결제 준비 중)'`. 메모리의 "KR ₩4,900"은 청구가가 아니라
표시 문구였다.

| 언어 | 유료 | 프로모 | 무료 |
|---|---|---|---|
| ko | **0** | 12 | 0 |
| ja | **0** | 2 | 4 |
| en | 5(테스트 4) | 1 | 0 |

**사용자의 절반이 돈을 낼 방법이 없었다.** 그리고 `deluxe` 티어도 상품이 아니었다 —
카탈로그에 항목이 없고 토큰 수 플래그(7000 vs 5000) 하나가 전부. 진단 문서 §3.2의
"코드는 이미 준비돼 있다"는 세 번째 "이미 됐다" 오류다.

### 3.4 가격 — 저항 데이터가 없으므로 잃을 것도 없다

$4.99는 카테고리(16Personalities $29, CliftonStrengths $25, Etsy 사주 리딩 $15~40)
대비 4~6배 아래였다. **작게 올리면 이 표본 수에서 측정 자체가 불가능**하므로
신호가 나올 만큼 크게 움직였다:

| 로케일 | 이전 | 현재 |
|---|---|---|
| en/zh/vi/id | $4.99 | **$19.99** |
| ja | ¥490 | **¥2,480** |
| es/pt/fr | €3.49 / €3.99 | **€17.99** |
| th | ฿89 | **฿449** |

**가격은 세 곳에 흩어져 있었고 이미 어긋나 있었다.** ja 배너는 "プレミアムレポートは
US$4.99"라고 광고하면서 결제는 ¥490을 받았고, schema.org Offer는 모든 언어에
4.99 USD를 검색엔진에 알리고 있었다. 기존 계약 테스트를 확장해 **"보여주는 숫자 ==
청구하는 숫자"를 로케일별로 검증**한다(옛 ja 불일치를 되살리면 실패하는 것 확인).

구 상품은 `LEGACY_PRODUCT_IDS`로 남겼다 — 지우면 **배포 순간 결제창이 떠 있던
주문이 capture에서 깨진다.**

"커피 한 잔 가격" 카피는 10개 언어에서 전부 걷어냈다. $19.99에서는 거짓말이 된다.

**판정 규칙(사전 확정):** 프리뷰 200건 누적 후 `checkout_start/preview`가 기준선의
절반 미만이면 되돌린다. 그 전에는 숫자를 보고 해석하지 않는다.

### 3.5 콘텐츠 엔진 — 두 소스의 품질이 달랐다

엔진을 돌려 보고 나서 신뢰했다. 손으로 쓴 뱅크 스니펫은 게시 가능했고, 블로그를
잘라 만든 것은 마크다운 조각이었다:

```
**What works:** - Project-based learning where they can follow their curiosity - Physical
```

5개 중 2개꼴. 완결 문장만 쓰고 마크다운을 벗기도록 고쳤다 — **18편 144건, 결함 0건**.

ko/ja는 **번역이 아니라 저작**했다. 잘린 영어를 번역하면 그 결함이 두 언어로
번지고 번역 품질 문제가 그 위에 얹힌다. 30일 캘린더 × 3언어 = 180건.

부수 발견: `formatDate`가 `toISOString`을 써서 KST에서 **모든 날짜가 하루 일찍**
찍히는데 요일 이름은 로컬이라 맞았다 — 달력이 자기 자신과 어긋나 있었다.

**X는 CJK를 2자로 센다** — 200자 한국어 게시물은 `.length`가 통과시켜도 280 한도
초과다. ko/ja는 가중 계산한다.

### 3.6 한국 결제 — 이미 80%가 진행돼 있었다 (Gmail)

세션 중 "한국 결제 레일은 사업자 결정이라 대신 정할 수 없다"고 했는데, **이미
7월에 정하고 신청까지 되어 있었다.** 2026-07-14: PortOne 가입 → 한국결제네트웍스·
NHN KCP·카카오페이 접수 완료, KCP 계약 안내 수신(초기등록비·연회비 면제).

막힌 곳 3개: ① 결제모듈 미구현(카드사 심사 전제) ② 보증보험 미가입
③ 사업자 정보 6가지 + 통신판매업 신고번호 사이트 하단 기재.

**실제 장벽은 절차가 아니라 노출이었다** — 유선번호가 없어 개인번호가, 사업장이
집이라 집주소가 공개된다. 이건 070 인터넷전화(월 2~5천원) + 비상주 사무실
(월 3~5만원)으로 **월 4~6만원에 해결되는 문제**다.

MoR(Paddle·Lemon Squeezy)은 과거 pseudo-science 사유로 거절당했다. Gmail 확인
결과 거절 메일 자체는 없고 LS 인증 메일 1건만 — 대시보드 단계 반려로 보인다.
재포장 재신청은 권하지 않는다(발각 시 정산 동결이 거절보다 나쁘다).

**결정: 현상 유지 + 트리거.** 한국은 무료+프로모(비용 0·노출 0), 매출은 EN/JA 집중.
**EN/JA 유료 월 10건 초과 시** 가상오피스+070 세팅 후 KCP 재신청.

그리고 **결제 의향 카운터**를 붙였다. ko 결과 페이지의 "카드 결제가 열리면
이용하고 싶어요" 버튼 → `purchase_intent`. 이게 없으면 한국 수요는 영원히
느낌으로만 남는다. 클라이언트발 엔드포인트는 **이 이벤트 하나만** 받는다 —
서버가 직접 세는 진실(preview/purchase)을 클라이언트가 쏠 수 있으면 curl 한 줄로
전환율이 오염된다.

### 3.7 미성년자 데이터 — 구조가 방어하고, 갭은 2개였다

10개 시장 조사(`docs/privacy-minor-data-review_2026-08-13.md`). 결론은 한 문장이다:
**사용자는 부모, 아이는 사용자가 아니며, 동의는 법정대리인이 직접 한다.** 이 구조가
COPPA(아동 대상 아님·아동으로부터 수집 없음), GDPR Art 8, 태국·베트남·인도네시아·
브라질의 부모동의 요건을 통과시킨다.

그리고 **문서를 믿지 않고 백엔드를 대조**했다. 갭 2건:

| 발견 | 실태 |
|---|---|
| **국외이전 동의가 별도로 안 남음** | 화면은 4개를 따로 물었는데 클라이언트가 개인정보처리 동의와 **AND로 합쳐** 전송. 합친 값에서 분리 불가 → "국외이전에 동의했는가"에 DB가 답할 수 없었다. 정작 그 동의가 아이 데이터를 OpenAI·Anthropic·Google로 보내는 근거인데 |
| **보존기간 삭제가 자동 실행 안 됨** | 365일 정책이 privacy policy에 **공개돼 있는데** 실행은 수동 CLI뿐. 사람이 기억해야 하는 약속이었고, 실패 방향은 아동 생년월일 무기한 축적 |

둘 다 당일 수정·배포. 국외이전 동의는 별도 필드(미전송은 `null`로 기록해 **거부와
구분** — false로 적으면 구버전 사용자가 거부자로 둔갑한다), 보존기간 청소는
매월 1일 EventBridge(라이브 드라이런 검증, 현재 대상 0건).

> 교훈: **공개한 약속과 코드를 대조한다.** 지키지 못할 약속을 공개해 두는 것이
> 약속을 안 하는 것보다 나쁘다.

---

## 4. 지금 상태

| 항목 | 상태 |
|---|---|
| 백엔드 | 최신 배포. 테스트 **255건 / 19 suites** |
| 프론트엔드 | 최신 배포(가격·카피·ko 의향 버튼 라이브) |
| migration | **009까지 실행 완료** (funnel_daily) |
| 스케줄 잡 | weekly-signals(월요일) + **retention-cleanup(매월 1일)** 둘 다 ENABLED |
| 퍼널 | 라이브 계측 중. 검증용 행은 전부 삭제, 실트래픽부터 집계 |
| 브랜치 | `codex/somyung-launch-ready` (main 미머지) |

### 새 자산
- `backend/src/services/funnel.service.js` — 퍼널 집계 (**AI 0**)
- `backend/migrations/009_funnel_counters.sql`
- `scripts/content-repurpose/templates.i18n.ts` — ko/ja 저작 뱅크
- `scripts/content-repurpose/export-markdown.ts` — 게시용 md 산출
- `docs/privacy-minor-data-review_2026-08-13.md` — 10개 시장 조사
- `docs/DPIA_2026-08-13.md` — 영향평가

---

## 5. 다음 세션에서 할 수 있는 것

### 5.1 기다리는 것
**퍼널이 쌓이기를 기다린다.** 판정선은 프리뷰 200건. 그 전에는 가격 판단을 하지
않는다. 주간 다이제스트가 월요일마다 온다.

### 5.2 신호가 말해주면
- 프리뷰 200 도달 → `checkout_start/preview`로 $19.99 판정
- **EN/JA 유료 월 10건 초과** → 한국 결제 레일 재개(가상오피스+070+KCP)
- ko `purchase_intent` 누적 → 위 판단의 보조 근거

### 5.3 신호와 무관하게 남은 것
- **D1: EU/UK 대표자(Art 27)** — EUR 판매 중이므로 의무 있음. EUR 매출 0이라
  미뤘고, DPIA §8에 "알고 미룸"으로 기록. **매출 발생 시 즉시 실행**
- **R3: 리포트 해석 한계 문구** — 리포트 본문에 있는지 점검, 없으면 추가
- 콘텐츠 실제 게시(사람 몫: SNS 계정)
- 랜딩 카피에 차별점 추가 — The Pattern의 "블랙박스" 비판에 대해 우리는 계산
  근거를 이미 보여준다. 마케팅 어디에도 안 쓰고 있다
- warmth 3.1 천장 (presentation 계약 확장 필요, 별도 결정)
- 백엔드 `npm run lint` 깨져 있음 — eslint 설정 없음

### 5.4 하지 않기로 유지
- **유료 광고** — $4.99에선 손익분기 전환율이 13~30%였다. $19.99에서 재계산
  가능하지만 퍼널 데이터가 먼저
- **MoR 재신청** — 정책 위반 리스크가 거절보다 크다
- **vi/id/th/zh 마케팅** — 사용자 0 + 미조사 법역. 베트남은 2026-01 신법 발효
- 중국 본토 — PIPL상 14세 미만 민감정보 + 점술 콘텐츠 규제 + 결제 불가

---

## 6. 절차로 남길 교훈

1. **저장소가 이미 아는 함정을 먼저 읽는다.** `report-job.js`가 경고한 Lambda
   fire-and-forget을 그대로 밟았다. 주석은 읽어야 작동한다.
2. **도구를 신뢰하기 전에 돌려 본다.** 콘텐츠 엔진은 산출물의 40%가 게시 불가였다.
   18편 × 3언어를 먼저 생성했으면 400건의 숙제가 됐다.
3. **문서가 아니라 코드를 본다.** 진단 문서의 "이미 준비돼 있다"가 세 번 틀렸고,
   privacy policy의 "365일 삭제"는 실행 경로가 없었다.
4. **중복 만들기 전에 있는 것을 찾는다.** 가격 정합 체커를 새로 만들었다가,
   같은 일을 하는 계약 테스트가 이미 있는 걸 보고 삭제하고 그쪽을 확장했다.
5. **합치면 되돌릴 수 없다.** 두 동의를 AND로 합친 순간 증적이 죽었다.
   법이 별개로 요구하는 것은 저장에서도 별개여야 한다.
6. **"없음"과 "모름"을 구분한다.** 국외이전 미전송을 `null`로 둔 이유이고,
   퍼널의 `measuringSince`가 있는 이유다.
